const P=storage.g19EventCard8006Payload;if(!P||P.payloadSha256!=="9ada6460e93ab362012b5e8164ed340054fd352983d1706873402b4e1e356d33")throw new Error('G19_PAYLOAD_NOT_VERIFIED');return await (async function installProductionRuntime(P){const FILE_ID='40e06342-8830-80d6-8008-8fc8a3a4cd4f';const PAGE_ID='c16498cb-b51d-8030-8008-904bd8fc9c53';const BOARD_ID='313fb1ed-0d5c-8095-8008-9108df52b2ce';const BOARD_NAME='KenigEvents · G12 bounded L0-L3';const ER=56;const EBC=16;const EBD=137;const ELC=15;const FAMILY='DejaVu Sans';const FONT_SOURCES=P.fontSources;const LEAF_PATH='KenigEvents / G19 / EventCard 8006 / Leaves';const CARD_PATH='KenigEvents / G19 / EventCard 8006 / Accepted';const GENERATION=19;const FIXTURE='event.real.8006+event.real.2182';const V2SHA='b1e236cf6e1faf59ba7e9de1cd4f6c2571349cae884b3f96f5f9743681a51330';const SHA=P.payloadSha256;const KP='kenigevents-payload-sha256',KM='kenigevents-g19-marker',KC='kenigevents-g19-child-marker',KI='kenigevents-instance-case-id';const KF='kenigevents-font-family',KW='kenigevents-font-weight',KS='kenigevents-font-style',KFS='kenigevents-font-source-sha256',KFR='kenigevents-font-runtime-id',KFV='kenigevents-font-variant-id';const V3SHA='c6c35b6f39e3cd5bc68bfe183c1df0652475533d4eecbaea8bd7bca1b4b35219';const LEGACY_V2_ICON_SHA256={icon:{not_interested:'d8d94023de0e563663c71a628657e3e4402ed5cb36fa836f784071e83edc8ae6',calendar:'f5465db33659eb80685704961006aa1d5f970f337dd6b330d8056c3326360633',share:'99103f01c0cbd48d87ff639dc3e6c6291a7f8c2aa147c854667d1a8f7a677cf9',like:'e5654867ef9431714cfc53a1890fb14fcaa52c64579388f5364a0fa01ce6ea58'}};const marker=(key)=>`kenigevents:g19:p2:${key}:v3`;const v2Marker=(key)=>`kenigevents:g19:p2:${key}:v2`;const fail=(code,detail={})=>{const error=new Error(`${code}: ${JSON.stringify(detail)}`);error.code=code;error.detail=detail;throw error;};function activeRun(){let control;try{control=JSON.parse(penpot.currentFile?.getSharedPluginData?.('kenigevents','asp-active-run-v1')||'null');}catch(error){fail('MATERIALIZATION_RUN_NOT_ACTIVE',{reason:'INVALID_JSON',message:String(error?.message||error)});}const expected=P.runControl;const exact=control?.schema==='kenigevents.asp-run-control.v1'&&control.run_id===expected.runId&&control.writer_id===expected.writerId&&control.state==='ACTIVE'&&control.contract_sha256===expected.contractSha256&&control.page_profile_sha256===expected.pageProfileSha256&&control.asset_registry_sha256===expected.assetRegistrySha256&&/^[0-9a-f]{64}$/.test(String(expected.geometryProofSha256||''))&&control.geometry_proof_sha256===expected.geometryProofSha256;if(!exact)fail('MATERIALIZATION_RUN_NOT_ACTIVE',{expected:{run_id:expected.runId,writer_id:expected.writerId,state:'ACTIVE',contract_sha256:expected.contractSha256,page_profile_sha256:expected.pageProfileSha256,asset_registry_sha256:expected.assetRegistrySha256,geometry_proof_sha256:expected.geometryProofSha256},actual:control});return control;}const write=(operation)=>{activeRun();return operation();};const writeAsync=async(operation)=>{activeRun();const result=await operation();activeRun();return result;};const array=(value)=>Array.from(value||[]);const round=(value)=>Math.round(Number(value)*1000)/1000;const eq=(a,b)=>Math.abs(Number(a)-Number(b))<=0.02;const plugin=(shape,key,value)=>write(()=>shape.setPluginData(key,String(value)));const children=(shape)=>array(shape?.children);const walk=(root)=>{const out=[],queue=root?[root]:[];while(queue.length){const shape=queue.shift();out.push(shape);queue.push(...children(shape));}return out;};const relativeX=(shape)=>Number(shape?.parentX??shape?.x??0);const relativeY=(shape)=>Number(shape?.parentY??shape?.y??0);const place=(shape,parent,box)=>{if(parent&&(!shape.parent||shape.parent.id!==parent.id))write(()=>parent.appendChild(shape));if(shape.layoutChild)write(()=>{shape.layoutChild.absolute=true;});write(()=>shape.resize(box.width,box.height));write(()=>penpotUtils.setParentXY(shape,box.x||0,box.y||0));return shape;};const setRadii=(shape,style={})=>{write(()=>{shape.borderRadiusTopLeft=style.radiusTL||0;});write(()=>{shape.borderRadiusTopRight=style.radiusTR||0;});write(()=>{shape.borderRadiusBottomRight=style.radiusBR||0;});write(()=>{shape.borderRadiusBottomLeft=style.radiusBL||0;});};const setFill=(shape,color,fillOpacity=1)=>{write(()=>{shape.fills=fillOpacity>0?[{fillColor:color,fillOpacity}]:[];});write(()=>{shape.strokes=[];});};const setStroke=(shape,style={})=>{write(()=>{shape.strokes=style.strokeWidth>0&&style.strokeOpacity>0?[{strokeColor:style.strokeColor,strokeOpacity:style.strokeOpacity,strokeStyle:'solid',strokeWidth:style.strokeWidth,strokeAlignment:'inner'}]:[];});};const auditRadii=(shape,style={},code='RADIUS_DRIFT',detail={})=>{if(!eq(shape.borderRadiusTopLeft||0,style.radiusTL||0)||!eq(shape.borderRadiusTopRight||0,style.radiusTR||0)||!eq(shape.borderRadiusBottomRight||0,style.radiusBR||0)||!eq(shape.borderRadiusBottomLeft||0,style.radiusBL||0))fail(code,detail);};const allComps=()=>array(penpot.library?.local?.components);const mainOf=(component)=>typeof component?.mainInstance==='function'?component.mainInstance():component?.mainInstance;const cm=(component,pathValue,name)=>{const main=mainOf(component);return component?.name===name&&[pathValue,''].includes(component.path)&&(component.path===pathValue||(main?.getPluginData?.(KM)===marker(name)&&main.getPluginData?.(KP)===SHA&&main.getPluginData?.('kenigevents-build-state')==='COMPLETE'&&main.getPluginData?.('kenigevents-component-name')===name&&main.getPluginData?.('kenigevents-component-id')===component.id&&main.parent?.id===BOARD_ID));};const componentOf=(main)=>allComps().find((component)=>mainOf(component)?.id===main?.id)||null;const findComp=(pathValue,name)=>allComps().find((component)=>cm(component,pathValue,name))||null;const targetBoard=()=>children(penpot.currentPage?.root).find((shape)=>shape.id===BOARD_ID)||null;const managedRoots=()=>children(targetBoard()).filter((shape)=>shape.getPluginData?.(KM));const findRoot=(key)=>managedRoots().find((shape)=>shape.getPluginData(KM)===marker(key))||null;function assertPrimitives(){if(typeof penpot==='undefined'||typeof penpotUtils==='undefined'||typeof storage==='undefined')fail('PENPOT_GLOBALS_MISSING');for(const name of['createBoard','createRectangle','createText','createShapeFromSvg','createShapeFromSvgWithImages']){if(typeof penpot[name]!=='function')fail('PENPOT_PRIMITIVE_MISSING',{name});}if(typeof penpotUtils.setParentXY!=='function')fail('PENPOT_SET_PARENT_XY_MISSING');if(typeof penpot.library?.local?.createComponent!=='function')fail('PENPOT_COMPONENT_API_MISSING');if(typeof penpot.currentFile?.validate!=='function')fail('PENPOT_VALIDATE_API_MISSING');if(typeof penpot.currentFile?.saveVersion!=='function')fail('PENPOT_SAVE_VERSION_API_MISSING');if(typeof penpot.currentFile?.findVersions!=='function')fail('PENPOT_FIND_VERSIONS_API_MISSING');if(typeof penpot.history?.undoBlockBegin!=='function'||typeof penpot.history?.undoBlockFinish!=='function')fail('PENPOT_UNDO_BLOCK_API_MISSING');if(!penpot.fonts||typeof penpot.fonts.findByName!=='function'||typeof penpot.fonts.findAllByName!=='function')fail('PENPOT_FONT_API_MISSING');}function context(){if(penpot.currentFile?.id!==FILE_ID||penpot.currentPage?.id!==PAGE_ID){fail('PENPOT_TARGET_MISMATCH',{expectedFile:FILE_ID,actualFile:penpot.currentFile?.id,expectedPage:PAGE_ID,actualPage:penpot.currentPage?.id});}const pageRoots=children(penpot.currentPage?.root);const board=targetBoard();if(pageRoots.length!==1||!board||board.name!==BOARD_NAME||board.type!=='board'){fail('PENPOT_ACCEPTED_BOARD_MISMATCH',{expectedBoardId:BOARD_ID,expectedBoardName:BOARD_NAME,pageRoots:pageRoots.map((shape)=>({id:shape.id,name:shape.name,type:shape.type}))});}const revision=Number(penpot.currentFile?.revn??penpot.currentFile?.revision);if(!Number.isFinite(revision))fail('PENPOT_REVISION_UNREADABLE');if(revision<ER)fail('PENPOT_REVISION_BEFORE_ACCEPTED_BASELINE',{minimum:ER,actual:revision});return board;}function baseline(){const board=context(),roots=children(board),components=allComps(),validation=validationResult();const managed=roots.filter((shape)=>shape.getPluginData?.(KM));if(!roots.length&&!components.length&&ER===41)return{mode:'TEST_FIXTURE_EMPTY_REVISION_41',revision:41,pageDirectRoots:1,boardId:board.id,boardChildren:0,boardDescendants:0,localComponents:0,validation};const revision=Number(penpot.currentFile?.revn??penpot.currentFile?.revision),descendants=walk(board).length-1;if(revision===ER&&(roots.length!==EBC||descendants!==EBD||components.length!==ELC))fail('PENPOT_BASELINE_CENSUS_MISMATCH',{revision,expected:{boardChildren:EBC,boardDescendants:EBD,localComponents:ELC},actual:{boardChildren:roots.length,boardDescendants:descendants,localComponents:components.length},validation});if(roots.length<EBC||roots.length>18||components.length<ELC||components.length>18||descendants<EBD)fail('PENPOT_RESUME_CENSUS_OUT_OF_BOUNDS',{revision,boardChildren:roots.length,boardDescendants:descendants,localComponents:components.length});if(managed.length!==roots.length)fail('UNMANAGED_ACCEPTED_BOARD_CHILDREN_PRESENT',{boardId:board.id,boardChildren:roots.length,managedRoots:managed.length});const rootIds=new Set(roots.map((shape)=>shape.id));if(components.some((component)=>!rootIds.has(mainOf(component)?.id)||!mainOf(component)?.getPluginData?.(KM)))fail('UNMANAGED_LOCAL_COMPONENTS_PRESENT');if(validation.length!==0)fail('PREEXISTING_VALIDATION_FAILURE',{validation});return{mode:revision===ER?'ACCEPTED_NATIVE_REVISION_56_MIXED_LINEAGE':'G19_V3_RESUME_OR_REUSE',revision,pageDirectRoots:1,boardId:board.id,boardChildren:roots.length,boardDescendants:descendants,localComponents:components.length,validation};}function resolveFonts(){const first=penpot.fonts.findByName(FAMILY);const fonts=[...new Set([first,...array(penpot.fonts.findAllByName(FAMILY))].filter(Boolean))];const rows=[];for(const font of fonts){const fontFamily=String(font?.fontFamily??font?.family??'');if(fontFamily!==FAMILY)continue;if(typeof font.applyToText!=='function')continue;for(const variant of array(font.variants)){const runtimeFontId=String(font?.id??font?.fontId??'');const variantId=String(variant?.id??variant?.fontVariantId??'');const weight=Number(variant?.fontWeight??variant?.weight);const style=String(variant?.fontStyle??variant?.style??'').toLowerCase();rows.push({font,variant,runtimeFontId,variantId,weight,style});}}const resolved={};for(const weight of[400,700]){const wantedVariant=`normal-${weight}`;const row=rows.find((candidate)=>candidate.weight===weight&&candidate.style==='normal'&&candidate.variantId===wantedVariant&&candidate.runtimeFontId);if(!row)fail('EXACT_NATIVE_FONT_VARIANT_MISSING',{family:FAMILY,weight,style:'normal',variantId:wantedVariant,available:rows.map((candidate)=>({runtimeFontId:candidate.runtimeFontId,variantId:candidate.variantId,weight:candidate.weight,style:candidate.style}))});resolved[weight]=row;}return resolved;}function stamp(shape,key,role){write(()=>{shape.name=key;});plugin(shape,KM,marker(key));plugin(shape,'kenigevents-generation',GENERATION);plugin(shape,'kenigevents-fixture-id',FIXTURE);plugin(shape,'kenigevents-role',role);plugin(shape,KP,SHA);plugin(shape,'kenigevents-accepted-roots-cleanup','forbidden');}const lineRatio=(style)=>String(Number((Number(style.lineHeight)/Number(style.fontSize)).toFixed(6)));function applyText(textShape,style,fontRows){const weight=700;const row=fontRows[weight];write(()=>row.font.applyToText(textShape,row.variant));write(()=>{textShape.growType='fixed';});write(()=>{textShape.fontSize=String(style.fontSize);});write(()=>{textShape.lineHeight=lineRatio(style);});write(()=>{textShape.letterSpacing=String(style.letterSpacing||0);});write(()=>{textShape.fills=[{fillColor:style.color,fillOpacity:style.colorOpacity}];});plugin(textShape,KF,FAMILY);plugin(textShape,KW,weight);plugin(textShape,KS,'normal');plugin(textShape,KFR,row.runtimeFontId);plugin(textShape,KFV,row.variantId);plugin(textShape,KFS,FONT_SOURCES[weight]);}function makeText(parent,name,value,box,style,fontRows){const text=write(()=>penpot.createText(value));write(()=>{text.name=name;});write(()=>{text.characters=value;});place(text,parent,box);applyText(text,style,fontRows);return text;}function tintedSvg(source,color){if(!source.includes('currentColor'))fail('ACTION_SVG_CURRENT_COLOR_CONTRACT_MISSING');return source.replaceAll('currentColor',color);}function icon(parent,svg,color,x,y,width,height,name){const shape=write(()=>penpot.createShapeFromSvg(tintedSvg(svg,color)));write(()=>{shape.name=name;});place(shape,parent,{x,y,width,height});return shape;}function createLeafRoot(spec){const root=write(()=>penpot.createBoard());stamp(root,spec.key,'leaf-master');write(()=>{root.clipContent=Boolean(spec.clip);});setFill(root,spec.fill||'#000000',spec.fillOpacity??0);setRadii(root,spec.style||{});if(spec.kind!=='media')setStroke(root,spec.style||{});place(root,targetBoard(),{...spec.box,x:spec.gallery.x,y:spec.gallery.y});plugin(root,'kenigevents-build-state','BUILDING');return root;}const V3I={payloadSha256:SHA,marker,childMarker:(rootKey,childKey)=>`${marker(rootKey)}:${childKey}`};const V2I={payloadSha256:V2SHA,marker:v2Marker,childMarker:(rootKey,childKey)=>`${v2Marker(rootKey)}:${childKey}`};const childMarker=V3I.childMarker;const findChild=(parent,rootKey,childKey,identity=V3I)=>{const matches=children(parent).filter((shape)=>shape.getPluginData?.(KC)===identity.childMarker(rootKey,childKey));if(matches.length>1)fail('DUPLICATE_MANAGED_CHILD',{rootKey,childKey,ids:matches.map((shape)=>shape.id)});return matches[0]||null;};const auditBox=(shape,box,code,detail={})=>{if(!shape||!eq(relativeX(shape),box.x||0)||!eq(relativeY(shape),box.y||0)||!eq(shape.width,box.width)||!eq(shape.height,box.height))fail(code,{...detail,expected:box,actual:shape&&{x:relativeX(shape),y:relativeY(shape),width:shape.width,height:shape.height}});};function stampChild(shape,rootKey,childKey){plugin(shape,KC,childMarker(rootKey,childKey));plugin(shape,KP,SHA);}function rectChild(parent,rootKey,childKey,box,color,fillOpacity=1,style=null,allow=true,identity=V3I){let shape=findChild(parent,rootKey,childKey,identity);if(!shape){if(!allow)fail('MANAGED_RECT_MISSING',{rootKey,childKey});shape=write(()=>penpot.createRectangle());if(!shape)fail('CREATE_RECTANGLE_FAILED',{rootKey,childKey});write(()=>{shape.name=childKey;});stampChild(shape,rootKey,childKey);setFill(shape,color,fillOpacity);if(style)setRadii(shape,style);place(shape,parent,box);}auditBox(shape,box,'MANAGED_RECT_GEOMETRY_DRIFT',{rootKey,childKey});const fill=array(shape.fills)[0];if(!fill||fill.fillColor!==color||!eq(fill.fillOpacity,fillOpacity)||array(shape.strokes).length||shape.getPluginData?.(KP)!==identity.payloadSha256)fail('MANAGED_RECT_STYLE_OR_PAYLOAD_DRIFT',{rootKey,childKey});if(style)auditRadii(shape,style,'MANAGED_RECT_RADIUS_DRIFT',{rootKey,childKey});return shape;}function ensureBoardChild(parent,rootKey,childKey,box,color,fillOpacity,style,allow=true,identity=V3I){let shape=findChild(parent,rootKey,childKey,identity);if(!shape){if(!allow)fail('MANAGED_BOARD_MISSING',{rootKey,childKey});shape=write(()=>penpot.createBoard());if(!shape)fail('CREATE_BOARD_FAILED',{rootKey,childKey});write(()=>{shape.name=childKey;});write(()=>{shape.clipContent=true;});stampChild(shape,rootKey,childKey);setFill(shape,color,fillOpacity);setRadii(shape,style);place(shape,parent,box);}auditBox(shape,box,'MANAGED_BOARD_GEOMETRY_DRIFT',{rootKey,childKey});const fill=array(shape.fills)[0];if(!fill||fill.fillColor!==color||!eq(fill.fillOpacity,fillOpacity)||array(shape.strokes).length||shape.clipContent!==true||shape.getPluginData?.(KP)!==identity.payloadSha256)fail('MANAGED_BOARD_STYLE_OR_PAYLOAD_DRIFT',{rootKey,childKey});auditRadii(shape,style,'MANAGED_BOARD_RADIUS_DRIFT',{rootKey,childKey});return shape;}function textChild(parent,rootKey,childKey,value,box,style,fontRows,allow=true,identity=V3I){let shape=findChild(parent,rootKey,childKey,identity);if(!shape){if(!allow)fail('MANAGED_TEXT_MISSING',{rootKey,childKey});shape=makeText(parent,childKey,value,box,style,fontRows);if(!shape)fail('CREATE_TEXT_FAILED',{rootKey,childKey});stampChild(shape,rootKey,childKey);}else if(allow){write(()=>{shape.characters=value;});place(shape,parent,box);applyText(shape,style,fontRows);}auditBox(shape,box,'MANAGED_TEXT_GEOMETRY_DRIFT',{rootKey,childKey});const fill=array(shape.fills)[0];if(shape.characters!==value||shape.getPluginData?.(KFR)!==fontRows[700].runtimeFontId||shape.getPluginData?.(KFV)!==fontRows[700].variantId||shape.getPluginData?.(KF)!==FAMILY||shape.getPluginData?.(KW)!=='700'||shape.getPluginData?.(KS)!=='normal'||shape.getPluginData?.(KFS)!==FONT_SOURCES[700]||shape.getPluginData?.(KP)!==identity.payloadSha256||!eq(shape.fontSize,style.fontSize)||!eq(shape.lineHeight,identity===V2I?style.lineHeight:lineRatio(style))||!eq(shape.letterSpacing||0,style.letterSpacing||0)||!fill||fill.fillColor!==style.color||!eq(fill.fillOpacity,style.colorOpacity))fail('MANAGED_TEXT_CONTENT_OR_FONT_DRIFT',{rootKey,childKey,value:shape.characters});return shape;}function ensureIconChild(parent,rootKey,childKey,source,color,box,assetSha256,allow=true,identity=V3I){let shape=findChild(parent,rootKey,childKey,identity);const action=rootKey.includes('not-interested')?'not_interested':rootKey.includes('calendar')?'calendar':rootKey.includes('share')?'share':rootKey.includes('like')?'like':null;const expectedAssetSha256=identity===V2I&&action?LEGACY_V2_ICON_SHA256.icon[action]:assetSha256;if(shape&&allow&&identity===V3I&&shape.getPluginData?.('kenigevents-svg-sha256')!==assetSha256){write(()=>shape.remove());shape=null;}if(!shape){if(!allow)fail('MANAGED_ICON_MISSING',{rootKey,childKey});shape=write(()=>penpot.createShapeFromSvg(tintedSvg(source,color)));if(!shape)fail('CREATE_SVG_FAILED',{rootKey,childKey});write(()=>{shape.name=childKey;});stampChild(shape,rootKey,childKey);plugin(shape,'kenigevents-svg-sha256',assetSha256);plugin(shape,'kenigevents-icon-color',color);place(shape,parent,box);}auditBox(shape,box,'MANAGED_ICON_GEOMETRY_DRIFT',{rootKey,childKey});if(shape.getPluginData?.('kenigevents-svg-sha256')!==expectedAssetSha256||shape.getPluginData?.('kenigevents-icon-color')!==color||shape.getPluginData?.(KP)!==identity.payloadSha256)fail('MANAGED_ICON_ASSET_OR_COLOR_DRIFT',{rootKey,childKey});return shape;}async function withUndo(fn){const blockId=write(()=>penpot.history.undoBlockBegin());try{return await fn();}finally{penpot.history.undoBlockFinish(blockId);}}const isV2=(root,key)=>root?.getPluginData?.(KM)===v2Marker(key)&&root.getPluginData?.(KP)===V2SHA;function migrateTree(root,rootKey,role){if(!isV2(root,rootKey)||root.getPluginData?.('kenigevents-build-state')!=='COMPLETE')fail('LEGACY_V2_ROOT_NOT_MIGRATABLE',{rootKey,rootId:root?.id||null});const oldPrefix=`${v2Marker(rootKey)}:`,newPrefix=`${marker(rootKey)}:`;for(const shape of walk(root).slice(1)){const childId=shape.getPluginData?.(KC)||'';const payload=shape.getPluginData?.(KP)||'';if(childId){if(!childId.startsWith(oldPrefix)||payload!==V2SHA)fail('LEGACY_V2_CHILD_NOT_MIGRATABLE',{rootKey,shapeId:shape.id,childId,payload});plugin(shape,KC,`${newPrefix}${childId.slice(oldPrefix.length)}`);plugin(shape,KP,SHA);}else if(payload){fail('LEGACY_V2_UNMARKED_PAYLOAD_DESCENDANT',{rootKey,shapeId:shape.id,payload});}}stamp(root,rootKey,role);plugin(root,'kenigevents-build-state','COMPLETE');}async function ensureComp(spec,build,auditLegacyV2){let component=findComp(spec.path,spec.name)||allComps().find((candidate)=>mainOf(candidate)?.getPluginData?.(KM)===marker(spec.key));if(component){const main=mainOf(component);if(isV2(main,spec.key)){if(typeof auditLegacyV2!=='function'||component.path!==spec.path||component.name!==spec.name||main.parent?.id!==BOARD_ID)fail('LEGACY_V2_COMPONENT_IDENTITY_COLLISION',{key:spec.key,componentId:component.id});return await withUndo(async()=>{await auditLegacyV2(main);const childN=spec.kind==='media'?3:spec.kind==='text-pill'?1:spec.inner.label&&spec.inner.count?3:2;if(children(main).length!==childN)fail('LEGACY_V2_LEAF_CHILD_CARDINALITY',{key:spec.key,expected:childN,actual:children(main).length});migrateTree(main,spec.key,'leaf-master');await build(main,false);await build(main,true);return{component,main,created:true,migratedFrom:'G19_V2',preservedIds:true};});}if(!main||main.getPluginData?.(KM)!==marker(spec.key))fail('COMPONENT_IDENTITY_COLLISION',{key:spec.key,name:spec.name});if((component.path!==spec.path||component.name!==spec.name)&&main.getPluginData('kenigevents-build-state')==='READY_FOR_COMPONENT'&&!component.path&&!component.name){return await withUndo(async()=>{await build(main,true);write(()=>{component.path=spec.path;});write(()=>{component.name=spec.name;});plugin(main,'kenigevents-component-name',spec.name);plugin(main,'kenigevents-component-id',component.id);plugin(main,'kenigevents-build-state','COMPLETE');return{component,main,created:true,resumedRegistration:true};});}if(!cm(component,spec.path,spec.name))fail('COMPONENT_REGISTRATION_INCOMPLETE',{key:spec.key,componentId:component.id,path:component.path,name:component.name});if(main.getPluginData('kenigevents-build-state')!=='COMPLETE'||main.getPluginData(KP)!==SHA)fail('COMPONENT_BUILD_STATE_DRIFT',{key:spec.key});auditBox(main,{x:relativeX(main),y:relativeY(main),width:spec.box.width,height:spec.box.height},'COMPONENT_GEOMETRY_DRIFT',{key:spec.key});await build(main,true);return{component,main,created:false};}return await withUndo(async()=>{let root=findRoot(spec.key);if(!root)root=createLeafRoot(spec);if(root.getPluginData(KP)!==SHA)fail('MANAGED_ROOT_PAYLOAD_DRIFT',{key:spec.key,rootId:root.id});await build(root,false);plugin(root,'kenigevents-build-state','READY_FOR_COMPONENT');component=write(()=>penpot.library.local.createComponent([root]));if(!component)fail('CREATE_COMPONENT_FAILED',{key:spec.key});write(()=>{component.path=spec.path;});write(()=>{component.name=spec.name;});plugin(root,'kenigevents-component-name',spec.name);plugin(root,'kenigevents-component-id',component.id);plugin(root,'kenigevents-build-state','COMPLETE');return{component,main:mainOf(component)||root,created:true};});}function leafSpecs(caseSpec,index){const slots=caseSpec.slots,viewport=caseSpec.viewport,inner=P.descendants[caseSpec.caseId];const baseX=1240+index*570;let galleryY=0;const next=(height)=>{const y=galleryY;galleryY+=height+24;return{x:baseX,y};};const specs=[];const add=(identity,slotName,kind,extra={})=>{const slot=slots[slotName],key=`${identity}.${viewport}.8006`;specs.push({key,name:key,semanticIdentity:identity,path:LEAF_PATH,viewport,slotName,kind,box:{x:0,y:0,width:slot.box.width,height:slot.box.height},gallery:next(slot.box.height),slot,...extra});};add('event.media-frame','media-link','media',{clip:true,fill:'#15110f',fillOpacity:0,style:slots['media-link'].style,mediaSlot:slots.image});add('event.meta.event-type','event-type','text-pill');add('event.meta.admission','admission','text-pill');add('event.action.not-interested','action.not_interested','action',{action:'not_interested',inner:inner.not_interested});add('event.action.calendar','action.calendar','action',{action:'calendar',inner:inner.calendar});add('event.action.share','action.share','action',{action:'share',inner:inner.share});add('event.action.like','action.like','action',{action:'like',inner:inner.like});return specs;}function leafSpecsAll(){const masters=['desktop','mobile'].map((viewport)=>P.cases.find((caseSpec)=>caseSpec.viewport===viewport&&caseSpec.variant.includes('wide')));if(masters.some((caseSpec)=>!caseSpec))fail('LEAF_MASTER_CASE_MISSING');return masters.flatMap((caseSpec,index)=>leafSpecs(caseSpec,index));}async function buildLeaf(spec,root,fontRows,auditOnly,identity=V3I){if(auditOnly){if(root.getPluginData('kenigevents-semantic-identity')!==spec.semanticIdentity||root.getPluginData('kenigevents-structural-context')!==spec.viewport)fail('LEAF_SEMANTIC_BINDING_DRIFT',{key:spec.key});}else{plugin(root,'kenigevents-semantic-identity',spec.semanticIdentity);plugin(root,'kenigevents-structural-context',spec.viewport);}const slot=spec.slot;const auditStyle=()=>{const fills=array(root.fills),fillAlpha=identity===V2I&&spec.kind==='media'?1:spec.fillOpacity??slot.style.backgroundOpacity??0;const expectedFillColor=spec.fill||slot.style.backgroundColor;if(fillAlpha>0&&(!fills[0]||fills[0].fillColor!==expectedFillColor||!eq(fills[0].fillOpacity,fillAlpha)))fail('LEAF_ROOT_FILL_DRIFT',{key:spec.key});if(fillAlpha===0&&fills.length)fail('LEAF_ROOT_FILL_DRIFT',{key:spec.key});auditRadii(root,spec.style||slot.style,'LEAF_ROOT_RADIUS_DRIFT',{key:spec.key});const expectedStroke=spec.kind!=='media'&&slot.style.strokeWidth>0&&slot.style.strokeOpacity>0;const strokes=array(root.strokes);if(expectedStroke&&(!strokes[0]||strokes[0].strokeColor!==slot.style.strokeColor||!eq(strokes[0].strokeOpacity,slot.style.strokeOpacity)||!eq(strokes[0].strokeWidth,slot.style.strokeWidth)))fail('LEAF_ROOT_STROKE_DRIFT',{key:spec.key});if(!expectedStroke&&strokes.length)fail('LEAF_ROOT_STROKE_DRIFT',{key:spec.key});if(root.getPluginData(KM)!==identity.marker(spec.key)||root.getPluginData(KP)!==identity.payloadSha256)fail('LEAF_ROOT_PAYLOAD_DRIFT',{key:spec.key});};if(spec.kind==='media'){if(!auditOnly)setFill(root,'#000000',0);rectChild(root,spec.key,'border.top',{x:0,y:0,width:slot.box.width,height:1},'#793014',0.13,null,!auditOnly,identity);rectChild(root,spec.key,'border.left',{x:0,y:0,width:1,height:slot.box.height},'#793014',0.13,null,!auditOnly,identity);rectChild(root,spec.key,'border.right',{x:slot.box.width-1,y:0,width:1,height:slot.box.height},'#793014',0.13,null,!auditOnly,identity);auditStyle();return;}if(spec.kind==='text-pill'){const fragment=slot.lineFragments[0];if(!fragment)fail('FROZEN_LINE_FRAGMENT_MISSING',{key:spec.key});const box={x:fragment.x-slot.box.x,y:fragment.y-slot.box.y,width:fragment.width,height:fragment.height};if(!auditOnly){setFill(root,slot.style.backgroundColor,slot.style.backgroundOpacity);setRadii(root,slot.style);setStroke(root,slot.style);}textChild(root,spec.key,'label',slot.text,box,slot.style,fontRows,!auditOnly,identity);auditStyle();return;}if(!auditOnly){if(slot.style.backgroundOpacity>0)setFill(root,slot.style.backgroundColor,slot.style.backgroundOpacity);setRadii(root,slot.style);setStroke(root,slot.style);}const colors=slot.style.color,geometry=spec.inner;const iconSource={not_interested:P.icons.notInterested,calendar:P.icons.calendar,share:P.icons.share,like:P.icons.like}[spec.action];const iconSha256={not_interested:P.iconSha256.notInterested,calendar:P.iconSha256.calendar,share:P.iconSha256.share,like:P.iconSha256.like}[spec.action];const labels={not_interested:'Не интересно',calendar:'В календарь',share:'Поделиться'};ensureIconChild(root,spec.key,'icon',iconSource,colors,geometry.icon,iconSha256,!auditOnly,identity);if(geometry.label)textChild(root,spec.key,'label',labels[spec.action],geometry.label,slot.style,fontRows,!auditOnly,identity);if(geometry.count)textChild(root,spec.key,'count',spec.action==='share'?'1':'9',geometry.count,slot.style,fontRows,!auditOnly,identity);auditStyle();}function createCardRoot(spec){const root=write(()=>penpot.createBoard());stamp(root,spec.key,'accepted-card-master');write(()=>{root.clipContent=false;});setFill(root,'#000000',0);setRadii(root,{radiusTL:24,radiusTR:24,radiusBR:24,radiusBL:24});place(root,targetBoard(),{...spec.box,x:spec.position.x,y:spec.position.y});plugin(root,'kenigevents-build-state','BUILDING');plugin(root,'kenigevents-semantic-root',spec.semanticRoot);plugin(root,'kenigevents-semantic-identity','component.event-card.free-collection');plugin(root,'kenigevents-structural-context',spec.structuralContext);plugin(root,'kenigevents-case-id',spec.caseId);return root;}function ensureLink(parent,rootKey,binding,leaf,allow=true,identity=V3I){let instance=findChild(parent,rootKey,binding.slotName,identity);if(!instance){if(!allow)fail('LINKED_INSTANCE_MISSING',{rootKey,slotName:binding.slotName});instance=write(()=>leaf.component.instance());if(!instance)fail('CREATE_COMPONENT_INSTANCE_FAILED',{rootKey,leafKey:binding.leafKey});write(()=>{instance.name=binding.slotName;});stampChild(instance,rootKey,binding.slotName);plugin(instance,'kenigevents-linked-leaf-key',binding.leafKey);plugin(instance,'kenigevents-linked-component-id',leaf.component.id);plugin(instance,'kenigevents-linked','true');place(instance,parent,binding.box);}auditBox(instance,binding.box,'LINKED_INSTANCE_GEOMETRY_DRIFT',{rootKey,slotName:binding.slotName});const actualComponent=typeof instance.component==='function'?instance.component():null;if(!linked(instance)||actualComponent?.id!==leaf.component.id||instance.getPluginData('kenigevents-linked-component-id')!==leaf.component.id)fail('LINKED_INSTANCE_LINEAGE_DRIFT',{rootKey,slotName:binding.slotName,expectedComponentId:leaf.component.id,actualComponentId:actualComponent?.id||null});return instance;}const nestedChild=(instance,leafKey,childKey,identity=V3I)=>walk(instance).slice(1).find((shape)=>shape.getPluginData?.(KC)===identity.childMarker(leafKey,childKey))||null;function currentMaster(instance,leafKey){const c=instance.component?.(),m=mainOf(c);return c?.id===instance.getPluginData?.('kenigevents-linked-component-id')&&c.name===leafKey&&[LEAF_PATH,''].includes(c.path)&&m?.getPluginData?.(KM)===marker(leafKey)&&m.getPluginData?.(KP)===SHA&&m.getPluginData?.('kenigevents-build-state')==='COMPLETE'&&m.getPluginData?.('kenigevents-component-name')===leafKey&&m.getPluginData?.('kenigevents-component-id')===c.id&&m.parent?.id===BOARD_ID?m:null;}function copyChild(instance,leafKey,childKey,identity){const v2=nestedChild(instance,leafKey,childKey,V2I);if(identity===V2I&&v2?.getPluginData?.(KP)===V2SHA)return v2;const v3=nestedChild(instance,leafKey,childKey);return v3?.getPluginData?.(KP)===SHA&&(identity===V3I||currentMaster(instance,leafKey))?v3:null;}function migrateCopy(instance,leafKey){const rootMarker=instance.getPluginData?.(KM)||'';const rootPayload=instance.getPluginData?.(KP)||'';const descendants=walk(instance).slice(1).filter((shape)=>shape.getPluginData?.(KC));const hasLegacyDescendants=descendants.some((shape)=>shape.getPluginData(KC).startsWith(`${v2Marker(leafKey)}:`));if(rootMarker===marker(leafKey)&&rootPayload===SHA&&!hasLegacyDescendants)return false;if(![marker(leafKey),v2Marker(leafKey)].includes(rootMarker)||![SHA,V2SHA].includes(rootPayload))fail('LINKED_COPY_ROOT_NOT_MIGRATABLE',{leafKey,instanceId:instance.id,rootMarker,rootPayload});for(const shape of descendants){const value=shape.getPluginData(KC);const payload=shape.getPluginData?.(KP)||'';if(value.startsWith(`${v2Marker(leafKey)}:`)){if(payload!==V2SHA)fail('LINKED_COPY_CHILD_NOT_MIGRATABLE',{leafKey,instanceId:instance.id,shapeId:shape.id,value,payload});plugin(shape,KC,`${marker(leafKey)}:${value.slice(`${v2Marker(leafKey)}:`.length)}`);plugin(shape,KP,SHA);}else if(!value.startsWith(`${marker(leafKey)}:`)||payload!==SHA){fail('LINKED_COPY_CHILD_IDENTITY_DRIFT',{leafKey,instanceId:instance.id,shapeId:shape.id,value,payload});}}plugin(instance,KM,marker(leafKey));plugin(instance,KP,SHA);return true;}function bindInstance(instance,spec,binding,fontRows,auditOnly,identity=V3I){const slot=spec.slots[binding.slotName];const leafKey=binding.leafKey;if(!auditOnly&&identity===V3I)migrateCopy(instance,leafKey);if(!auditOnly){plugin(instance,KI,spec.caseId);plugin(instance,'kenigevents-instance-slot',binding.slotName);if(binding.slotName==='media-link')setFill(instance,'#000000',0);else if(slot.style.backgroundOpacity>0)setFill(instance,slot.style.backgroundColor,slot.style.backgroundOpacity);else setFill(instance,'#000000',0);setRadii(instance,slot.style);setStroke(instance,slot.style);}if(instance.getPluginData?.(KI)!==spec.caseId||instance.getPluginData?.('kenigevents-instance-slot')!==binding.slotName)fail('LINKED_INSTANCE_CASE_BINDING_DRIFT',{caseId:spec.caseId,slot:binding.slotName});if(binding.slotName==='media-link'){const fills=array(instance.fills);const m=currentMaster(instance,leafKey),opaque=fills.length===1&&fills[0].fillColor==='#15110f'&&eq(fills[0].fillOpacity,1);const inherited=fills.length===0&&m&&array(m.fills).length===0&&children(m).length===3;const ownV2=instance.getPluginData?.(KM)===v2Marker(leafKey)&&instance.getPluginData?.(KP)===V2SHA;const ownV3=instance.getPluginData?.(KM)===marker(leafKey)&&instance.getPluginData?.(KP)===SHA;if(identity===V2I?!(ownV2&&(opaque||inherited)):!(ownV3&&fills.length===0))fail('LINKED_MEDIA_FRAME_FILL_DRIFT',{caseId:spec.caseId,fills});return;}if(binding.slotName==='event-type'||binding.slotName==='admission'){const label=copyChild(instance,leafKey,'label',identity),fragment=slot.lineFragments[0];if(!label||!fragment)fail('LINKED_TEXT_OVERRIDE_TARGET_MISSING',{caseId:spec.caseId,slot:binding.slotName});const box={x:fragment.x-slot.box.x,y:fragment.y-slot.box.y,width:fragment.width,height:fragment.height};if(!auditOnly){write(()=>{label.characters=slot.text;});place(label,instance,box);applyText(label,slot.style,fontRows);plugin(label,KI,spec.caseId);}auditBox(label,box,'LINKED_TEXT_OVERRIDE_GEOMETRY_DRIFT',{caseId:spec.caseId,slot:binding.slotName});if(label.characters!==slot.text||label.getPluginData?.(KI)!==spec.caseId||label.getPluginData?.(KFR)!==fontRows[700].runtimeFontId||label.getPluginData?.(KFV)!==fontRows[700].variantId)fail('LINKED_TEXT_OVERRIDE_CONTENT_DRIFT',{caseId:spec.caseId,slot:binding.slotName});return;}const action=binding.slotName.replace('action.',''),geometry=P.descendants[spec.caseId]?.[action];if(!geometry)fail('CASE_ACTION_DESCENDANT_EVIDENCE_MISSING',{caseId:spec.caseId,action});const fixture=P.fixtures[spec.fixtureId];const values={label:{not_interested:'Не интересно',calendar:'В календарь',share:'Поделиться'}[action],count:action==='share'?String(fixture.shares):String(fixture.likes)};for(const part of['icon','label','count']){if(!(part in geometry))continue;const child=copyChild(instance,leafKey,part,identity);if(!child)fail('LINKED_ACTION_OVERRIDE_TARGET_MISSING',{caseId:spec.caseId,action,part});const expected=geometry[part],hidden=Boolean(expected.hidden);if(!auditOnly){write(()=>{child.hidden=hidden;});plugin(child,KI,spec.caseId);if(!hidden)place(child,instance,expected);if(part!=='icon'){write(()=>{child.characters=values[part];});applyText(child,slot.style,fontRows);}}const caseId=child.getPluginData?.(KI)||'';const m=part==='icon'&&identity===V2I?currentMaster(instance,leafKey):null;const mi=m?nestedChild(m,leafKey,'icon'):null;const iconSha={not_interested:P.iconSha256.notInterested,calendar:P.iconSha256.calendar,share:P.iconSha256.share,like:P.iconSha256.like}[action];const inherited=caseId===''&&child.getPluginData?.(KC)===childMarker(leafKey,'icon')&&child.getPluginData?.(KP)===SHA&&child.getPluginData?.('kenigevents-svg-sha256')===iconSha&&mi?.getPluginData?.('kenigevents-svg-sha256')===iconSha&&(mi.getPluginData?.(KI)||'')==='';if(Boolean(child.hidden)!==hidden||(caseId!==spec.caseId&&!inherited))fail('LINKED_ACTION_OVERRIDE_STATE_DRIFT',{caseId:spec.caseId,action,part});if(!hidden)auditBox(child,expected,'LINKED_ACTION_OVERRIDE_GEOMETRY_DRIFT',{caseId:spec.caseId,action,part});if(!hidden&&part!=='icon'&&child.characters!==values[part])fail('LINKED_ACTION_OVERRIDE_CONTENT_DRIFT',{caseId:spec.caseId,action,part});}}async function ensureMedia(root,spec,auditOnly,identity=V3I){const childKey='content.media-artwork',box=spec.slots.image.box,media=P.media[spec.fixtureId];if(!media)fail('CASE_MEDIA_PAYLOAD_MISSING',{caseId:spec.caseId,fixtureId:spec.fixtureId});let shape=findChild(root,spec.key,childKey,identity);if(!shape){if(auditOnly)fail('CASE_MEDIA_SHAPE_MISSING',{caseId:spec.caseId});const aspect=media.fit==='cover'?'xMidYMid slice':'xMidYMid meet';const svg=`<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" width="${box.width}" height="${box.height}" viewBox="0 0 ${box.width} ${box.height}"><defs><clipPath id="clip"><rect width="${box.width}" height="${box.height}"/></clipPath></defs><rect width="${box.width}" height="${box.height}" fill="#15110f"/><image clip-path="url(#clip)" x="0" y="0" width="${box.width}" height="${box.height}" preserveAspectRatio="${aspect}" href="data:image/webp;base64,${media.base64}" xlink:href="data:image/webp;base64,${media.base64}"/></svg>`;shape=await writeAsync(()=>penpot.createShapeFromSvgWithImages(svg));if(!shape)fail('CREATE_CASE_MEDIA_SVG_FAILED',{caseId:spec.caseId});write(()=>{shape.name=`${spec.fixtureId}.poster.${media.fit}.50-50`;});stampChild(shape,spec.key,childKey);plugin(shape,'kenigevents-media-sha256',media.sha256);plugin(shape,'kenigevents-media-fit',media.fit);plugin(shape,'kenigevents-media-position','50% 50%');plugin(shape,'kenigevents-fixture-id',spec.fixtureId);place(shape,root,box);}auditBox(shape,box,'CASE_MEDIA_GEOMETRY_DRIFT',{caseId:spec.caseId});if(shape.getPluginData?.('kenigevents-media-sha256')!==media.sha256||shape.getPluginData?.('kenigevents-media-fit')!==media.fit||shape.getPluginData?.('kenigevents-media-position')!=='50% 50%'||shape.getPluginData?.('kenigevents-fixture-id')!==spec.fixtureId||shape.getPluginData?.(KP)!==identity.payloadSha256)fail('CASE_MEDIA_BINDING_DRIFT',{caseId:spec.caseId});return shape;}async function buildStatic(spec,root,fontRows,auditOnly,identity=V3I){auditRadii(root,{radiusTL:24,radiusTR:24,radiusBR:24,radiusBL:24},'CARD_ROOT_RADIUS_DRIFT',{key:spec.key});if(array(root.fills).length||array(root.strokes).length||root.clipContent!==false||root.getPluginData(KM)!==identity.marker(spec.key)||root.getPluginData(KP)!==identity.payloadSha256)fail('CARD_ROOT_STYLE_OR_PAYLOAD_DRIFT',{key:spec.key});rectChild(root,spec.key,'surface.body',spec.slots.body.box,'#15110f',1,null,!auditOnly,identity);const u=spec.slots['utility-row'].box;const utility=ensureBoardChild(root,spec.key,'surface.utility-row',u,'#15110f',1,spec.slots['utility-row'].style,!auditOnly,identity);rectChild(root,spec.key,'body.border.left',{x:spec.slots.body.box.x,y:spec.slots.body.box.y,width:1,height:spec.slots.body.box.height},'#793014',0.13,null,!auditOnly,identity);rectChild(root,spec.key,'body.border.right',{x:spec.slots.body.box.width-1,y:spec.slots.body.box.y,width:1,height:spec.slots.body.box.height},'#793014',0.13,null,!auditOnly,identity);rectChild(utility,spec.key,'utility.border.left',{x:0,y:0,width:1,height:u.height},'#793014',0.13,null,!auditOnly,identity);rectChild(utility,spec.key,'utility.border.right',{x:u.width-1,y:0,width:1,height:u.height},'#793014',0.13,null,!auditOnly,identity);rectChild(utility,spec.key,'utility.border.bottom',{x:0,y:u.height-1,width:u.width,height:1},'#793014',0.13,null,!auditOnly,identity);for(const slotName of['title','occurrence','place']){const slot=spec.slots[slotName];textChild(root,spec.key,slotName,slot.text,slot.box,slot.style,fontRows,!auditOnly,identity);}await ensureMedia(root,spec,auditOnly,identity);}function bindCard(spec,root,leaves,fontRows,auditOnly,role,identity=V3I){const selected=spec.bindings.filter((binding)=>role==='shell'?['media-link','event-type','admission'].includes(binding.slotName):!['media-link','event-type','admission'].includes(binding.slotName));for(const binding of selected){const leaf=leaves[binding.leafKey];if(!leaf)fail('LEAF_COMPONENT_MISSING',{leafKey:binding.leafKey});const instance=ensureLink(root,spec.key,binding,leaf,!auditOnly,identity);bindInstance(instance,spec,binding,fontRows,auditOnly,identity);}}async function auditCard(spec,root,leaves,fontRows,identity=V3I){await buildStatic(spec,root,fontRows,true,identity);bindCard(spec,root,leaves,fontRows,true,'shell',identity);bindCard(spec,root,leaves,fontRows,true,'actions',identity);}async function migrateLegacyV2Card(component,spec,root,leaves,fontRows){if(!isV2(root,spec.key)||component.path!==CARD_PATH||component.name!==spec.name||root.parent?.id!==BOARD_ID||root.getPluginData?.('kenigevents-build-state')!=='COMPLETE')fail('LEGACY_V2_CARD_NOT_MIGRATABLE',{key:spec.key,componentId:component.id,rootId:root.id});if(root.getPluginData?.('kenigevents-semantic-root')!==spec.semanticRoot||root.getPluginData?.('kenigevents-semantic-identity')!=='component.event-card.free-collection'||root.getPluginData?.('kenigevents-structural-context')!==spec.structuralContext||root.getPluginData?.('kenigevents-case-id')!==spec.caseId)fail('LEGACY_V2_CARD_SEMANTIC_DRIFT',{key:spec.key});await auditCard(spec,root,leaves,fontRows,V2I);const expectedN=8+spec.bindings.length;if(children(root).length!==expectedN)fail('LEGACY_V2_CARD_CHILD_CARDINALITY',{key:spec.key,expected:expectedN,actual:children(root).length});return await withUndo(async()=>{const oldCardPrefix=`${v2Marker(spec.key)}:`,newCardPrefix=`${marker(spec.key)}:`;for(const shape of walk(root).slice(1)){const value=shape.getPluginData?.(KC)||'';if(!value.startsWith(oldCardPrefix))continue;if(shape.getPluginData?.(KP)!==V2SHA)fail('LEGACY_V2_CARD_CHILD_PAYLOAD_DRIFT',{key:spec.key,shapeId:shape.id,value});plugin(shape,KC,`${newCardPrefix}${value.slice(oldCardPrefix.length)}`);plugin(shape,KP,SHA);}stamp(root,spec.key,'accepted-card-master');plugin(root,'kenigevents-build-state','COMPLETE');for(const binding of spec.bindings){const instance=findChild(root,spec.key,binding.slotName);if(!instance)fail('MIGRATED_CARD_LINKED_INSTANCE_MISSING',{key:spec.key,slot:binding.slotName});migrateCopy(instance,binding.leafKey);place(instance,root,binding.box);bindInstance(instance,spec,binding,fontRows,false);}await buildStatic(spec,root,fontRows,false);await auditCard(spec,root,leaves,fontRows);return{component,main:root,created:true,state:'COMPLETE',migratedFrom:'G19_V2',preservedIds:true};});}function migrateLiveShell(root,spec){if(spec.key!=='eventcard.desktop-packed-calendar-absent.2182'||root.id!=='313fb1ed-0d5c-8095-8008-914c76615924'||root.parent?.id!==BOARD_ID||root.getPluginData?.(KM)!==marker(spec.key)||root.getPluginData?.(KP)!==V3SHA||root.getPluginData?.('kenigevents-build-state')!=='BUILDING'||children(root).length!==10||walk(root).length-1!==21)fail('LIVE_V3_PARTIAL_ROOT_NOT_MIGRATABLE',{key:spec.key,rootId:root?.id||null,payload:root?.getPluginData?.(KP)||null,state:root?.getPluginData?.('kenigevents-build-state')||null,directChildren:children(root).length,descendants:walk(root).length-1});const prefix=`${marker(spec.key)}:`;for(const shape of walk(root).slice(1)){const cardChildMarker=shape.getPluginData?.(KC)||'';if(!cardChildMarker.startsWith(prefix))continue;if(shape.getPluginData?.(KP)!==V3SHA)fail('LIVE_V3_PARTIAL_CHILD_PAYLOAD_DRIFT',{key:spec.key,shapeId:shape.id,cardChildMarker});plugin(shape,KP,SHA);}stamp(root,spec.key,'accepted-card-master');plugin(root,'kenigevents-build-state','BUILDING');}async function cardShell(spec,leaves,fontRows){let component=findComp(CARD_PATH,spec.name)||allComps().find((candidate)=>mainOf(candidate)?.getPluginData?.(KM)===marker(spec.key));if(component){const main=mainOf(component);if(isV2(main,spec.key))return migrateLegacyV2Card(component,spec,main,leaves,fontRows);if(!main||!cm(component,CARD_PATH,spec.name)||main.getPluginData('kenigevents-build-state')!=='COMPLETE')fail('CARD_COMPONENT_IDENTITY_COLLISION',{key:spec.key});auditBox(main,{x:relativeX(main),y:relativeY(main),width:spec.box.width,height:spec.box.height},'CARD_ROOT_GEOMETRY_DRIFT',{key:spec.key});await auditCard(spec,main,leaves,fontRows);return{component,main,created:false,state:'COMPLETE'};}return await withUndo(async()=>{let root=findRoot(spec.key);if(!root)root=createCardRoot(spec);else if(root.getPluginData?.(KP)===V3SHA)migrateLiveShell(root,spec);const state=root.getPluginData('kenigevents-build-state');if(!['BUILDING','SHELL_COMPLETE'].includes(state))fail('CARD_SHELL_PARTIAL_STATE_UNKNOWN',{key:spec.key,state});await buildStatic(spec,root,fontRows,state==='SHELL_COMPLETE');bindCard(spec,root,leaves,fontRows,state==='SHELL_COMPLETE','shell');plugin(root,'kenigevents-build-state','SHELL_COMPLETE');return{component:null,main:root,created:state!=='SHELL_COMPLETE',state:'SHELL_COMPLETE'};});}async function cardFinal(spec,leaves,fontRows){const existing=findComp(CARD_PATH,spec.name)||allComps().find((candidate)=>mainOf(candidate)?.getPluginData?.(KM)===marker(spec.key));if(existing){const main=mainOf(existing);const resumableRegistration=main?.getPluginData?.('kenigevents-build-state')==='SHELL_COMPLETE'&&main.getPluginData?.(KP)===SHA&&['',CARD_PATH].includes(existing.path)&&['',spec.name].includes(existing.name);if(resumableRegistration)return await withUndo(async()=>{await auditCard(spec,main,leaves,fontRows);write(()=>{existing.path=CARD_PATH;});write(()=>{existing.name=spec.name;});plugin(main,'kenigevents-component-name',spec.name);plugin(main,'kenigevents-component-id',existing.id);plugin(main,'kenigevents-build-state','COMPLETE');return{component:existing,main,created:true,state:'COMPLETE',resumedRegistration:true};});if(!main||!cm(existing,CARD_PATH,spec.name)||main.getPluginData('kenigevents-build-state')!=='COMPLETE')fail('CARD_COMPONENT_IDENTITY_COLLISION',{key:spec.key});await auditCard(spec,main,leaves,fontRows);return{component:existing,main,created:false,state:'COMPLETE'};}return await withUndo(async()=>{const root=findRoot(spec.key);if(!root||root.getPluginData('kenigevents-build-state')!=='SHELL_COMPLETE')fail('CARD_SHELL_NOT_COMPLETE',{key:spec.key,state:root?.getPluginData?.('kenigevents-build-state')||null});await buildStatic(spec,root,fontRows,true);bindCard(spec,root,leaves,fontRows,true,'shell');bindCard(spec,root,leaves,fontRows,false,'actions');const component=write(()=>penpot.library.local.createComponent([root]));if(!component)fail('CREATE_CARD_COMPONENT_FAILED',{key:spec.key});write(()=>{component.path=CARD_PATH;});write(()=>{component.name=spec.name;});plugin(root,'kenigevents-component-name',spec.name);plugin(root,'kenigevents-component-id',component.id);plugin(root,'kenigevents-build-state','COMPLETE');return{component,main:mainOf(component)||root,created:true,state:'COMPLETE'};});}function cardSpecs(){return P.cases.map((caseSpec,index)=>{const viewport=caseSpec.viewport;const suffix=`${viewport}.8006`;const slotToLeaf={'media-link':`event.media-frame.${suffix}`,'event-type':`event.meta.event-type.${suffix}`,admission:`event.meta.admission.${suffix}`,'action.not_interested':`event.action.not-interested.${suffix}`,'action.calendar':`event.action.calendar.${suffix}`,'action.share':`event.action.share.${suffix}`,'action.like':`event.action.like.${suffix}`,};return{key:caseSpec.caseId,name:caseSpec.caseId,caseId:caseSpec.caseId,fixtureId:caseSpec.fixtureId,semanticRoot:`kenigevents.free-collection.${caseSpec.caseId}`,viewport,structuralContext:caseSpec.variant,box:caseSpec.box,slots:caseSpec.slots,position:{x:(index%2)*620,y:Math.floor(index/2)*1040},bindings:Object.entries(slotToLeaf).filter(([slotName])=>caseSpec.slots[slotName]).map(([slotName,leafKey])=>({slotName,leafKey,box:caseSpec.slots[slotName].box})),};});}function linked(instance){if(typeof instance?.isComponentCopyInstance==='function')return Boolean(instance.isComponentCopyInstance());if(typeof instance?.component==='function')return Boolean(instance.component());return Boolean(instance?.componentId||instance?.getPluginData?.('kenigevents-linked')==='true');}function validationResult(){const result=penpot.currentFile.validate();return Array.isArray(result)?result:result==null?[]:result;}function readback(strict=false){const board=context(),fontRows=resolveFonts();const leafSpecsRows=leafSpecsAll();const cardSpecsAll=cardSpecs();const expectedLeafNames=leafSpecsRows.map((spec)=>spec.name),cardNames=cardSpecsAll.map((spec)=>spec.name);const names=[...expectedLeafNames,...cardNames],expectedMarkers=new Set([...leafSpecsRows.map((spec)=>marker(spec.key)),...cardSpecsAll.map((spec)=>marker(spec.key))]);const issues=[];const components=names.map((name)=>{const pathValue=cardNames.includes(name)?CARD_PATH:LEAF_PATH;const matches=allComps().filter((component)=>cm(component,pathValue,name));const component=matches[0],main=mainOf(component);if(matches.length>1||(strict&&matches.length!==1))issues.push({code:'COMPONENT_CARDINALITY',name,count:matches.length});if(main&&(main.getPluginData?.('kenigevents-build-state')!=='COMPLETE'||main.getPluginData?.(KP)!==SHA||main.parent?.id!==BOARD_ID))issues.push({code:'COMPONENT_STATE_PAYLOAD_OR_PARENT',name,parentId:main.parent?.id||null});return{name,nativePath:component?.path??null,expectedPath:pathValue,componentId:component?.id||null,rootId:main?.id||null,marker:main?.getPluginData?.(KM)||null,width:main?round(main.width):null,height:main?round(main.height):null,directChildCount:main?children(main).length:null};});for(const spec of leafSpecsRows){const component=findComp(LEAF_PATH,spec.name),main=mainOf(component);if(main&&(!eq(main.width,spec.box.width)||!eq(main.height,spec.box.height)))issues.push({code:'LEAF_ROOT_GEOMETRY',name:spec.name});const childN=spec.kind==='media'?3:spec.kind==='text-pill'?1:spec.inner.label&&spec.inner.count?3:2;if(main&&children(main).length!==childN)issues.push({code:'LEAF_CHILD_CARDINALITY',name:spec.name,expected:childN,actual:children(main).length});if(main&&walk(main).filter((shape)=>shape.type==='text').some((shape)=>shape.getPluginData?.(KFR)!==fontRows[700].runtimeFontId||shape.getPluginData?.(KFV)!==fontRows[700].variantId))issues.push({code:'LEAF_TEXT_FONT',name:spec.name});}const cards=cardSpecsAll.map((spec)=>{const component=findComp(CARD_PATH,spec.name),main=mainOf(component);const links=main?children(main).filter(linked):[],textShapes=main?walk(main).filter((shape)=>shape.type==='text'):[],text=textShapes.map((shape)=>shape.characters);if(main&&(!eq(main.width,spec.box.width)||!eq(main.height,spec.box.height)))issues.push({code:'CARD_ROOT_GEOMETRY',name:spec.name});const expectedN=8+spec.bindings.length;if(main&&children(main).length!==expectedN)issues.push({code:'CARD_CHILD_CARDINALITY',name:spec.name,expected:expectedN,actual:children(main).length});if(main&&links.length!==spec.bindings.length)issues.push({code:'CARD_LINKED_LEAF_COUNT',name:spec.name,expected:spec.bindings.length,actual:links.length});if(main)for(const binding of spec.bindings){const instance=findChild(main,spec.key,binding.slotName);if(!instance||!eq(relativeX(instance),binding.box.x)||!eq(relativeY(instance),binding.box.y)||!eq(instance.width,binding.box.width)||!eq(instance.height,binding.box.height))issues.push({code:'CARD_SLOT_GEOMETRY',name:spec.name,slot:binding.slotName});}const fixture=P.fixtures[spec.fixtureId],requiredText=[fixture.title,fixture.type,fixture.occurrence,fixture.admission,fixture.place,'Не интересно','Поделиться',...(spec.slots['action.calendar']?['В календарь']:[])];if(main)for(const value of requiredText)if(!text.includes(value))issues.push({code:'CARD_TEXT_MISSING',name:spec.name,value});if(main&&textShapes.filter((shape)=>!shape.hidden).some((shape)=>shape.getPluginData?.(KFR)!==fontRows[700].runtimeFontId||shape.getPluginData?.(KFV)!==fontRows[700].variantId))issues.push({code:'CARD_TEXT_FONT',name:spec.name});return{name:spec.name,fixtureId:spec.fixtureId,semanticRoot:main?.getPluginData?.('kenigevents-semantic-root')||null,structuralContext:main?.getPluginData?.('kenigevents-structural-context')||null,componentId:component?.id||null,rootId:main?.id||null,width:main?round(main.width):null,height:main?round(main.height):null,linkedLeafCount:links.length,linkedInstances:links.map((instance)=>({instanceId:instance.id,slot:instance.getPluginData?.('kenigevents-instance-slot')||instance.name,componentId:instance.component?.()?.id||null})),text};});const roots=managedRoots(),rootMarkers=roots.map((root)=>root.getPluginData(KM));const semanticTuples=roots.map((root)=>`${root.getPluginData('kenigevents-semantic-identity')}|${root.getPluginData('kenigevents-structural-context')}`);const mainOfIds=new Set(allComps().map((component)=>mainOf(component)?.id).filter(Boolean));const markerDuplicates=rootMarkers.length-new Set(rootMarkers).size;const unexpectedMarkers=rootMarkers.filter((value)=>!expectedMarkers.has(value));const nameDuplicates=names.reduce((count,name)=>count+Math.max(0,allComps().filter((component)=>component.name===name&&(component.path===CARD_PATH||component.path===LEAF_PATH)).length-1),0);const semanticDuplicates=semanticTuples.length-new Set(semanticTuples).size;const duplicates=markerDuplicates+nameDuplicates+semanticDuplicates+unexpectedMarkers.length;if(strict&&roots.length!==names.length)issues.push({code:'MANAGED_ROOT_COUNT',expected:names.length,actual:roots.length});if(strict&&allComps().length!==names.length)issues.push({code:'LOCAL_COMPONENT_COUNT',expected:names.length,actual:allComps().length});const validation=validationResult();const pendingRoots=roots.filter((root)=>!mainOfIds.has(root.id));const resumableStates=new Set(['BUILDING','READY_FOR_COMPONENT','SHELL_COMPLETE']);const detachedRoots=pendingRoots.filter((root)=>!resumableStates.has(root.getPluginData?.('kenigevents-build-state')));return{schema:'kenigevents.penpot.g19.eventcard-four-case.readback.v2',fileId:penpot.currentFile.id,pageId:penpot.currentPage.id,revision:penpot.currentFile.revn??penpot.currentFile.revision??null,fixtureId:FIXTURE,payloadSha256:SHA,expectedBaselineRevision:ER,pageDirectRootCount:children(penpot.currentPage.root).length,board:{id:board.id,name:board.name,childCount:children(board).length,descendantCount:walk(board).length-1},managedBoardChildCount:roots.length,expectedManagedBoardChildCount:names.length,acceptedCardRootCount:cards.filter((card)=>card.rootId).length,expectedAcceptedCardRootCount:4,expectedManagedComponentCount:names.length,managedComponentCount:components.filter((component)=>component.componentId).length,totalLocalComponentCount:allComps().length,inProgressRootCount:pendingRoots.length,pendingRoots:pendingRoots.map((root)=>({id:root.id,name:root.name,state:root.getPluginData?.('kenigevents-build-state')||null})),detachedRootCount:detachedRoots.length,screenshotRootCount:roots.filter((root)=>/screenshot/i.test(root.name)||root.getPluginData?.('kenigevents-role')==='screenshot').length,routeLocalDuplicateMasterCount:duplicates,fontBinding:{family:FAMILY,sourceBindingSha256:P.fontSourceBindingSha256,regular:{runtimeFontId:fontRows[400].runtimeFontId,variantId:fontRows[400].variantId,weight:400,style:'normal',sourceSha256:FONT_SOURCES[400]},bold:{runtimeFontId:fontRows[700].runtimeFontId,variantId:fontRows[700].variantId,weight:700,style:'normal',sourceSha256:FONT_SOURCES[700]}},components,cards,auditIssues:issues,validation,};}function base64(bytes){const alphabet='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';let out='';for(let i=0;i<bytes.length;i+=3){const a=bytes[i],b=i+1<bytes.length?bytes[i+1]:0,c=i+2<bytes.length?bytes[i+2]:0;const n=(a<<16)|(b<<8)|c;out+=alphabet[(n>>>18)&63]+alphabet[(n>>>12)&63]+(i+1<bytes.length?alphabet[(n>>>6)&63]:'=')+(i+2<bytes.length?alphabet[n&63]:'=');}return out;}async function exportedBytes(value){if(value instanceof Uint8Array)return value;if(value instanceof ArrayBuffer)return new Uint8Array(value);if(ArrayBuffer.isView(value))return new Uint8Array(value.buffer,value.byteOffset,value.byteLength);if(value&&typeof value.arrayBuffer==='function')return new Uint8Array(await value.arrayBuffer());if(typeof value==='string'&&value.startsWith('data:')){return{dataUrl:value,base64:value.slice(value.indexOf(',')+1)};}fail('UNSUPPORTED_PNG_EXPORT_RESULT',{type:typeof value});}async function exportRoots(){context();const exports=[];for(const caseSpec of P.cases){const component=findComp(CARD_PATH,caseSpec.caseId),root=mainOf(component);if(!root)fail('ACCEPTED_CARD_ROOT_MISSING',{name:caseSpec.caseId});if(typeof root.export!=='function')fail('PENPOT_EXPORT_API_MISSING',{rootId:root.id});const raw=await root.export({type:'png'});const converted=await exportedBytes(raw);const encoded=converted.base64||base64(converted);exports.push({name:caseSpec.caseId,rootId:root.id,componentId:component.id,mime_type:'image/png',byte_length:converted.byteLength??null,base64:encoded,data_url:converted.dataUrl||`data:image/png;base64,${encoded}`});}return{schema:'kenigevents.penpot.g19.eventcard-four-case.png-exports.v2',fileId:FILE_ID,pageId:PAGE_ID,boardId:BOARD_ID,exports};}const PHASE_ORDER=['P10_DESKTOP_LEAVES_A','P11_DESKTOP_LEAVES_B','P20_MOBILE_LEAVES_A','P21_MOBILE_LEAVES_B','P30_DESKTOP_WIDE_SHELL','P31_DESKTOP_WIDE_FINAL','P40_DESKTOP_PACKED_SHELL','P41_DESKTOP_PACKED_FINAL','P50_MOBILE_WIDE_SHELL','P51_MOBILE_WIDE_FINAL','P60_MOBILE_PACKED_SHELL','P61_MOBILE_PACKED_FINAL','P90_FINALIZE',];const previousPhase=(phaseId)=>PHASE_ORDER[PHASE_ORDER.indexOf(phaseId)-1]||null;const phaseLeafSpecs=(phaseId)=>{const specs=leafSpecsAll(),desktop=specs.filter((spec)=>spec.viewport==='desktop'),mobile=specs.filter((spec)=>spec.viewport==='mobile');return({P10_DESKTOP_LEAVES_A:desktop.slice(0,4),P11_DESKTOP_LEAVES_B:desktop.slice(4),P20_MOBILE_LEAVES_A:mobile.slice(0,4),P21_MOBILE_LEAVES_B:mobile.slice(4)})[phaseId]||null;};const phaseCardSpec=(phaseId)=>{const map={P30_DESKTOP_WIDE_SHELL:'eventcard.desktop-wide-calendar.8006',P31_DESKTOP_WIDE_FINAL:'eventcard.desktop-wide-calendar.8006',P40_DESKTOP_PACKED_SHELL:'eventcard.desktop-packed-calendar-absent.2182',P41_DESKTOP_PACKED_FINAL:'eventcard.desktop-packed-calendar-absent.2182',P50_MOBILE_WIDE_SHELL:'eventcard.mobile-wide-calendar.8006',P51_MOBILE_WIDE_FINAL:'eventcard.mobile-wide-calendar.8006',P60_MOBILE_PACKED_SHELL:'eventcard.mobile-packed-calendar-absent.2182',P61_MOBILE_PACKED_FINAL:'eventcard.mobile-packed-calendar-absent.2182',};return cardSpecs().find((spec)=>spec.caseId===map[phaseId])||null;};const phaseComplete=(phaseId)=>{const leafRows=phaseLeafSpecs(phaseId);if(leafRows)return leafRows.every((spec)=>{const component=findComp(LEAF_PATH,spec.name),root=mainOf(component);return Boolean(component&&root?.getPluginData?.(KM)===marker(spec.key)&&root.getPluginData?.(KP)===SHA&&root.getPluginData?.('kenigevents-build-state')==='COMPLETE');});const card=phaseCardSpec(phaseId);if(card){const component=findComp(CARD_PATH,card.name),root=mainOf(component)||findRoot(card.key);const current=root?.getPluginData?.(KM)===marker(card.key)&&root.getPluginData?.(KP)===SHA;return phaseId.endsWith('_FINAL')?Boolean(component&&current&&root?.getPluginData?.('kenigevents-build-state')==='COMPLETE'):Boolean(current&&['SHELL_COMPLETE','COMPLETE'].includes(root.getPluginData?.('kenigevents-build-state')));}if(phaseId==='P90_FINALIZE')return allComps().length===18&&managedRoots().length===18;return false;};const loadLeaves=()=>Object.fromEntries(leafSpecsAll().map((spec)=>{const component=findComp(LEAF_PATH,spec.name),main=mainOf(component);if(!component||!main||main.getPluginData?.('kenigevents-build-state')!=='COMPLETE')fail('PHASE_LEAF_DEPENDENCY_MISSING',{leaf:spec.name});return[spec.key,{component,main,created:false}];}));async function saveVersion(phaseId,changed){const label=`G19 P2 V3 ${phaseId} · ${SHA.slice(0, 12)}`;const versions=array(await penpot.currentFile.findVersions());let version=versions.find((candidate)=>(candidate?.label||candidate?.name)===label)||null;const created=Boolean(changed||!version);if(created)version=await writeAsync(()=>penpot.currentFile.saveVersion(label));return{id:version?.id||null,label:version?.label||version?.name||label,created};}function gate(rb){const legacyRoots=managedRoots().filter((root)=>{const value=root.getPluginData?.(KM)||'';return value.endsWith(':v2')&&root.getPluginData?.(KP)===V2SHA;});if(!legacyRoots.length)return{active:false,accepted:rb.routeLocalDuplicateMasterCount===0&&rb.auditIssues.length===0,legacyRootNames:[]};const names=new Set(legacyRoots.map((root)=>root.getPluginData('kenigevents-component-name')));const allowed=rb.auditIssues.every((issue)=>names.has(issue.name)&&['COMPONENT_STATE_PAYLOAD_OR_PARENT','CARD_SLOT_GEOMETRY'].includes(issue.code));return{active:true,accepted:allowed&&rb.routeLocalDuplicateMasterCount===legacyRoots.length,legacyRootNames:[...names]};}async function runPhase(phaseId){if(!PHASE_ORDER.includes(phaseId))fail('UNKNOWN_MATERIALIZATION_PHASE',{phaseId,allowed:PHASE_ORDER});assertPrimitives();const runLease=activeRun();const preflight=baseline();const fontRows=resolveFonts();const dependency=previousPhase(phaseId);if(dependency&&!phaseComplete(dependency))fail('MATERIALIZATION_PHASE_DEPENDENCY_MISSING',{phaseId,dependency});const created=[],reused=[];const leafRows=phaseLeafSpecs(phaseId);if(leafRows){for(const spec of leafRows){const result=await ensureComp(spec,(root,auditOnly)=>buildLeaf(spec,root,fontRows,auditOnly),(root)=>buildLeaf(spec,root,fontRows,true,V2I),);(result.created?created:reused).push({role:'leaf',name:spec.name,componentId:result.component.id,rootId:result.main.id});}}else if(phaseId!=='P90_FINALIZE'){const spec=phaseCardSpec(phaseId),leaves=loadLeaves();const result=phaseId.endsWith('_SHELL')?await cardShell(spec,leaves,fontRows):await cardFinal(spec,leaves,fontRows);(result.created?created:reused).push({role:phaseId.endsWith('_SHELL')?'accepted-card-shell':'accepted-card',name:spec.name,componentId:result.component?.id||null,rootId:result.main.id,state:result.state});}const strict=phaseId==='P90_FINALIZE';const beforeSave=readback(strict);const beforeMigration=gate(beforeSave);if(beforeSave.screenshotRootCount!==0||beforeSave.validation.length!==0||!beforeMigration.accepted||(strict&&(beforeMigration.active||beforeSave.detachedRootCount!==0||beforeSave.acceptedCardRootCount!==4||beforeSave.managedComponentCount!==18||beforeSave.managedBoardChildCount!==18||beforeSave.totalLocalComponentCount!==18)))fail('POST_PHASE_ACCEPTANCE_FAILED',{phaseId,migration:beforeMigration,readback:beforeSave});const savedVersion=await saveVersion(phaseId,created.length>0);const result=readback(strict);const afterMigration=gate(result);if(result.validation.length||!afterMigration.accepted)fail('POST_PHASE_SAVE_ACCEPTANCE_FAILED',{phaseId,migration:afterMigration,readback:result});const terminalLease=activeRun();const mutatedObjectIds=created.flatMap((row)=>row.mutatedObjectIds||[]);return{schema:'kenigevents.penpot.g19.eventcard-four-case.phase-receipt.v3',phaseId,terminalState:created.length?'SUCCEEDED':'SUCCEEDED_IDEMPOTENT_REUSE',mutations:mutatedObjectIds.length||created.length,mutatedObjectIds,runControl:{run_id:runLease.run_id,writer_id:runLease.writer_id,state:terminalLease.state,contract_sha256:runLease.contract_sha256,page_profile_sha256:runLease.page_profile_sha256,asset_registry_sha256:runLease.asset_registry_sha256,geometry_proof_sha256:runLease.geometry_proof_sha256},preflight,created,reused,migration:afterMigration,savedVersion,completedPhases:PHASE_ORDER.filter(phaseComplete),pendingPhases:PHASE_ORDER.filter((id)=>!phaseComplete(id)),readback:result};}const api={runPhase,readback,exportRoots,constants:{FILE_ID,PAGE_ID,BOARD_ID,BOARD_NAME,ER,EBC,EBD,ELC,FAMILY,FONT_SOURCES,LEAF_PATH,CARD_PATH,FIXTURE,PHASE_ORDER}};storage.g19EventCard8006=api;return{schema:'kenigevents.penpot.g19.eventcard-four-case.runtime-ready.v2',installed:true,payloadSha256:SHA,phaseOrder:PHASE_ORDER,preflight:baseline(),fontBinding:readback(false).fontBinding};})(P);
