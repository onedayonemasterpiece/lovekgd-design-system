# Date Listing + Shell v1 candidate

First bounded vertical slice for archetype.listing.date. All artifacts remain candidate, noncanonical, nonpromoted, and nondeployed.

Ordered evidence follows fixture-manifest.v1.json representations. The source event projection is generated from immutable Golden Event Corpus v1 and verified by tests/date-listing-shell-v1.test.mjs.

## Current Penpot materialization

- `60.1 — Date Listing + Shell v1 · Components` — bounded component masters;
- `60.2 — Date Listing · Archetype masters` — linked `today`, `tomorrow`, and `specific` variants;
- `60.3 — Date Listing · Typical desktop` — same-data diagnostic board for representation 1.

The desktop flow is chronological and single-column. This is a semantic composition rule rather than a per-proof override. Review proofs may carry fixture-content text overlays while the frozen ListingEventCard dependency remains linked; such overlays are evidence-only and never become insertable library duplicates.
