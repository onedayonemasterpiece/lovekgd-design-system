# U-FREE-SHELL-REVIEW-PAGE-R1 — native successor R2

Directive: `RD-U0-U-FREE-SHELL-REVIEW-PAGE-R1` from
`D0_EXECUTABLE_BUFFER_V1` comment `5499373802`.

## Outcome

- Replaced the successor execution path with concrete native page, root,
  product component masters, visible anatomy, and linked state compositions.
- Kept all seven package-owned shell components and six frozen review scenarios.
- Bound every component master to exact source role/path/blob tuples.
- Corrected the Brandbook tree to
  `29ad3ccf0628e448d0881007129981b9f766856f` and Medallions tree to
  `95ab14cbd64697910c871ccb1a7ca7428cf618bd`.
- Added the exact `CollectionCatalog` source role/path/blob used by Breadcrumbs.
- Strict shared plugin-data accepts strings only; neither the runtime nor test
  double performs implicit string coercion.
- A real second invocation against the same native-like document creates zero
  nodes and preserves the full first-run projection.
- Duplicate, detached-instance, screenshot, foreign-page, cancelled-lease, and
  protected-projection gates fail closed.
- The existing Atlas extension request remains byte-identical; page ordering is
  still `O0_ONLY` and is not assigned by this package.

## Boundary

No Penpot service/API/tool was called, no Penpot data was read or mutated,
PUBLISH was not run, Atlas R2 was not changed, and Kaggle was not used. The
native-like QA result is not Penpot authorization.

Terminal package status: `ATLAS_EXTENSION_PENDING`.
