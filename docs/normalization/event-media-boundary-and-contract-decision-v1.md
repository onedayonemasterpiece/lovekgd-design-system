# Event Media boundary and contract decision v1

## Verdict

This package completes an evidence-bound **boundary model**, not a component acceptance or readiness promotion.

- Boundary result: `EVENT_MEDIA_BOUNDARY_MODEL_COMPLETE`
- Readiness result: `EVENT_MEDIA_NOT_READY_WITH_EXACT_BLOCKERS`
- Candidate decisions: `NOT_MERGED`
- Candidate contracts accepted: `false`
- Strict-ready candidates: `0 / 3`
- Product Value: `observe`; evidence status `pending_product_model`; promotion `false`
- Penpot materialization, prototype mutation, production UI mutation, normalization, migration, merge and deploy: **not authorized**

`BOUNDARY_MODEL_COMPLETE` means the evidence has been classified into explicit identities, compositions, placements, semantic modes, foundations and unresolved boundaries. It must not be shortened to “Event Media is ready”.

## Immutable authority

| Authority | Exact binding |
|---|---|
| Design decision base | `45288b001d724e0d3603d0c44d392ff370407bd0` |
| events-bot-new closure | `66bc0d43e36299417626f992021cfb7299ddf704` |
| events closure tree | `72e24f49ad6642915131438de8c56b804c4826b0` |
| events `site/src` tree | `d737458f8a87a9b7dad4f4badffd1b3f4ce544dd` |
| events `site/public` tree | `f42a045ec9ff3b1b2f3396a4df9f54cc6a767934` |
| Decoder v1 tree / manifest | `e77fc2457fadfdffb46ed2d90304ebb91e89a715` / `f7740f7f533c3f0cda5d4d0b8ebe98b565d7f521368b96462daecbd26522d5cc` |
| Component-decoder tree | `c2af02d0796f91b7510422db9b9e6179c70f606e` |
| Behavioral v1.1 manifest | `c676be4f2ad956b8a58c7707c8f71b7bb33afd771e506457309597e76d67d9a1` |
| Event Media dossier | `935c9ffd99cfe0e0e7f0625d20473991f2b5946d77be21666b5e47a4a76923d4` |
| Exact source-blocker digest | `77c1a288030173d1d1c25dd932dbf1ba8e576bc195702172a2c1b202981f7a46` |
| Penpot / prototypes trees | `b5cc94d35586f34554e8873e3a4380111057116d` / `658fa4cbc942a1e8cc34c005cf3305a944b8e86f` |

The validator rejects changed immutable bytes or trees. The external events checkout must be clean and exactly at the closure commit; screenshots remain evidence only and are not treated as production observation.

## Census and semantic boundary

The consumer matrix has exactly **52** independently addressable application rows: 37 active current-source production/social consumers and 15 current-source non-production consumers. Lifecycle detail is 37 production-reachable but not production-observed, 1 deprecated/preserved, 2 hidden/inert/preserved and 12 controlled specimens. It covers Event Detail, EventCard, search, listing, personal feed, favorites, collections, festival/club/artifact adjacent evidence, sharing and labs. Every row retains routes, source component at the exact events commit, semantic assignments, ratio evidence, geometry, fit, crop permission, focal point, safe area, object position, tiny/upscale behavior, missing/broken fallback, loading/layout reservation, responsive art direction, accessibility, runtime limits and cell-level provenance.

The semantic catalog has exactly **23** rows: 18 semantic/placement/state/output records and 5 explicit adjacent-boundary exclusions.

| Axis | Rows |
|---|---:|
| asset semantics | 5 |
| placement semantics | 8 |
| state semantics | 3 |
| output semantics | 2 |
| boundary exclusions | 5 |

Photography, portrait poster, textual artwork, OCR/document and unknown-text-protected semantics remain separate. OCR/document and unknown-text-protected candidate policies default to `contain` with crop forbidden. Unknown or erroneous classification cannot fail open to cover/crop.

### Ratios

Every consumer retains its observed provenance; no global ratio or token is selected. The six mandatory vocabulary entries are `4:5`, `5:4`, `3:2`, `2:3`, `1:1` and `intrinsic/source`. The exact observed set additionally contains:

`11:13`, `16:10`, `27:20`, `29:26`, `31:56..199:112`, `3:4`, `40:21`, `4:3`, `6:5`, `intrinsic/source-clamped-0.2..3.2`, `intrinsic/source-clamped-0.38..2.2`, `intrinsic/source-or-bounded-row`, `not-fixed/grid-cell-width-by-count`, `not-fixed/grid-row-min-height`, `not-fixed/responsive-target-unknown`, `not-fixed/viewport-derived`, `source-or-generated-4:5` and `viewport-cover/unspecified-source`.

These values are evidence, not target decisions.

## Boundary model and candidate ownership

The boundary model has exactly **31** rows: 23 semantic forward-reference boundaries, 1 analytical-family boundary, 3 candidate-identity boundaries, 2 foundation boundaries and 2 unresolved internal boundaries. The reconciled entity kinds are:

| Entity kind | Rows |
|---|---:|
| consumer placement | 8 |
| composition pattern | 6 |
| unresolved boundary | 4 |
| semantic media mode | 6 |
| component identity candidate | 3 |
| foundation candidate | 2 |
| subcomponent candidate | 1 |
| implementation detail | 1 |

`family.event-media` remains a `composition_pattern` and owns no component contract. Only the following three `component_identity_candidate` records own draft candidate files:

| Candidate | Evidence-bound role | Consumer refs | Exact blockers |
|---|---|---:|---:|
| `candidate.event-primary-media` | primary event-detail image-or-fallback frame with semantic fit and optional viewer-open behavior | 6 | 11 |
| `candidate.event-media-viewer` | modal source-order viewer with accessible interaction | 6 | 11 |
| `candidate.event-fallback-art` | deterministic decorative no-image art for selected event categories | 6 | 6 |

All three remain `candidate-not-accepted`, non-canonical, draft, blocked, unmaterialized in Penpot and unauthorized for physical work. Placements, rails and analytical compositions do not acquire candidate contracts by proximity.

Every boundary compares the same six alternatives: `merge`, `preserve_as_variant`, `preserve_as_composition`, `split`, `preserve_product_pattern` and `preserve_unresolved`. Across the 31 evidence-bound recommendations the distribution is 1 merge, 7 variant, 3 composition, 5 split, 10 product-pattern and 5 unresolved. Recommendations are reversible evidence assessments, not accepted owner decisions.

## Exact blocker ledger

The closure ledger preserves all **12** source statements exactly. Nine remain `still_open`; three require owner decisions. Field presence alone never closes a blocker, controlled evidence does not claim production state, and every row retains findings plus limitations.

| ID | Status | Exact source statement |
|---|---|---|
| `EM-CENSUS-001` | `owner_decision_required` | No accepted family boundary distinguishes Event Detail primary/companion/previews from related EventCard, mobile listing media, and the lab-only rail. |
| `EM-RATIO-002` | `still_open` | 4:5, 5:4, 3:2, 2:3, 1:1, intrinsic/source and other observed ratios are not positively bound or explicitly non-applicable for every accepted consumer. |
| `EM-SEMANTIC-003` | `still_open` | Photography, poster/artwork, OCR/document, unknown-text and classified non-photo document behavior is not reconciled across Desktop, EventHero and EventMediaRail. |
| `EM-CROP-004` | `still_open` | Focal point, safe area, crop permission and object-position are not one proven policy; metadata exists but focal/safe data does not gate current cover. |
| `EM-TINY-005` | `still_open` | No accepted upscale ceiling exists, and EventHero low-resolution containment is logically unreachable. |
| `EM-FALLBACK-006` | `still_open` | Missing fallback exists, but broken-image convergence is unproven for Event Detail primary, companion, gallery and previews. |
| `EM-LAYOUT-007` | `still_open` | Mobile primary/poster reservation and genuine lazy gallery loading treatment are incomplete; skeleton applicability is not adjudicated for every slot. |
| `EM-RESP-008` | `still_open` | No complete responsive art-direction contract exists; primary media lacks alternate-source art direction and nine Desktop cascade probes are unreconciled. |
| `EM-RUNTIME-009` | `still_open` | All relevant supplement captures are candidate or controlled runtime; none is production-equivalent or production-observed. |
| `EM-GOV-010` | `owner_decision_required` | Human review remains pending, normalization is disallowed, recommendation is unresolved and no owner decision receipt exists. |
| `EM-LABRAIL-011` | `owner_decision_required` | Lab-only EventMediaRail covers OCR/documents and lacks broken/missing/tiny plus overflow/keyboard closure. |
| `EM-PROVENANCE-012` | `still_open` | Historical dossier has global provenance lists, not reconciled source + requirement + runtime evidence per consumer cell. |

The two queue items remain `PENDING_OWNER_DECISION`:

1. `identity-set-v1`: whether to preserve exactly the three evidence-backed identities while keeping all other boundaries outside component identity. The evidence recommendation is `preserve-three-candidate-split`.
2. `viewer-rail-boundary-v1`: whether the standard viewer remains the only viewer candidate while gallery previews stay compositions and the efficient portrait viewer plus lab rail remain unresolved/evidence-only. The evidence recommendation is `preserve-bounded-separation`.

Neither recommendation is accepted; both decision receipts are absent.

## Positive readiness recomputation

Each of the three candidates has the exact 23-dimension checklist: 11 `PASS`, 11 `BLOCKED` and 1 `NOT_APPLICABLE_WITH_REASON`. The validator recomputes `not_ready_reason_codes` from blocked dimensions, joins closure/open/owner blocker sets, verifies candidate bytes, and fails closed if a blocked candidate claims readiness.

| Candidate | Still-open evidence blockers | Owner-decision blocker refs | Result |
|---|---:|---:|---|
| primary media | 9 | 2 | `NOT_READY_WITH_EXACT_BLOCKERS` |
| media viewer | 8 | 3 | `NOT_READY_WITH_EXACT_BLOCKERS` |
| fallback art | 4 | 2 | `NOT_READY_WITH_EXACT_BLOCKERS` |

The surrounding baseline is unchanged: 47 analytical groups, 107 component memberships, 47 `NOT_READY`, 0 scored, 0 strict-ready and 0 first-wave. All 239 application/value-readiness rows remain `pending_product_model` with promotion false. All six pinned experiments remain `NOT_MERGED`; no winner receipt exists.

## Machine contracts and delivery

- Catalog rows: `contracts/normalization/event-media-contract-decision-catalog.v1.schema.json` (JSON Schema Draft 2020-12).
- Candidate contracts: `contracts/normalization/event-media-candidate-contract.v1.schema.json` (three files).
- Receipt: `contracts/normalization/event-media-contract-decision-receipt.v1.schema.json`.
- Semantic validator: `scripts/validate-event-media-contract-decision-v1.mjs`.
- Deterministic receipt builder/checker: `scripts/build-event-media-contract-decision-v1-receipt.mjs`; default is check-only and `--write` is deliberate.
- Negative validation: exactly 60 named mutations, including fail-closed boundary/candidate/readiness identity and owner-blocker joins, stable `EMV_*` errors, aggregate rejection and baseline restoration.
- Workflow: `.github/workflows/event-media-contract-decision-v1.yml`, pinned runtime/actions, exact events ancestry, no secrets, no merge/deploy.

The receipt excludes itself from its output inventory and records each other output's bytes, SHA-256 and JSONL record count. It binds immutable authority, exact counts, final non-ready statuses, STOP constraints and Draft PR metadata. `committed_receipt_asserts_ci` is false: workflow execution is external evidence and cannot be self-asserted by committed JSON.

Delivery is **Draft PR only**. The final receipt is materialized only after the Draft PR number and canonical URL exist; review does not imply merge authorization.

### Validation commands

Before the final receipt exists, the deliberate local validation is:

```bash
python3 scripts/validate-event-media-contract-decision-schemas-v1.py --root . --skip-receipt
node scripts/validate-event-media-contract-decision-v1.mjs \
  --root . --events-repo /path/to/events-bot-new-at-66bc0d43 --skip-receipt
node tests/event-media-contract-decision-v1-negative.mjs
node tests/event-media-contract-decision-v1-workflow-path-filters.mjs
```

Expected facts are 52 / 23 / 31 / 12 / 3 / 31 / 3 / 2 and 23 checks per candidate; the semantic command must report only the two final statuses at the top of this document. After Draft metadata is known, `--write --pr-number N --pr-url URL` materializes the receipt once; schema, semantic and builder commands then run without `--skip-receipt` or `--write`.

## Legacy proof bridge

The v1.1 aggregate receipt was materialized before these bounded outputs existed, so asking it to inventory current-head files would guarantee a false red result. The replay keeps the original proof intact by:

1. checking out exact design commit `45288b001d724e0d3603d0c44d392ff370407bd0` and running the legacy v1.1 validator **with receipt validation enabled**; then
2. running the current-head legacy semantic corpus with explicit `--skip-receipt`.

This does not weaken the frozen proof or transfer its claims to current outputs. The new Event Media receipt and workflow own current delivery validation.

## STOP

Until separate owner decision and promotion receipts exist, do not:

- accept or canonicalize any candidate;
- merge/split/delete implementations, normalize APIs or start migration;
- choose a global ratio/token, weaken OCR/unknown contain/no-crop, or infer identity from visual similarity;
- create Product Value IDs/metrics or promote beyond `observe` / `pending_product_model`;
- select an experiment winner;
- mutate Penpot, prototypes, events production source or UI;
- merge or deploy this Draft decision package.
