# EventCard Media standalone bundle v1

Package-local successor for `MAT-EVENTCARD-MEDIA-SAME-TUPLE-EXECUTION-R3`.
It exposes `globalThis.KenigEventsD0EventcardMediaR3StandaloneV2` as one browser/Penpot-safe JavaScript artifact with fresh four-target projection, four in-place fill execution and distinct coverage settlement entrypoints. The exact two WebP inputs and a portable SHA-256 implementation are embedded; no repository loader, Node built-ins or publisher-side source copying is required. Exact bundle bytes and SHA-256 are repeated in the authorization and provenance tuple. Penpot execution remains independent QA/INTEGRATE, fresh-projection and bounded ACTIVE gated; Text is unchanged and remains blocked.

The V2 global uses a deterministic plain-data clone for projection values and never invokes the Penpot sandbox `structuredClone` host function.
