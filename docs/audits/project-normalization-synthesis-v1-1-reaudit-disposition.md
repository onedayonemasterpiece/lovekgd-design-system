# Project Normalization Synthesis v1.1 — independent re-audit disposition

## Authority and interpretation

The independent re-audit is preserved without edits at
[`project-normalization-synthesis-v1-1-independent-red-team-reaudit.md`](project-normalization-synthesis-v1-1-independent-red-team-reaudit.md):
**61,775 bytes**, SHA-256
`7dfdb90abc7798a0c3c69db8d818f16ef803571bcac4ac32b921fd1514db3b41`.
It remains the verdict authority for the audited head
`bcdff9de56663bb77f15f32660ab0156c937e77b`.

This ledger accepts all and only the six new findings from that report. Acceptance
means that the correction is required; it is not a claim that the correction has
already been implemented, validated, independently reviewed, or authorized for
merge. Evidence for a future reconciled head belongs in the separate v1.1.1
proof-closure report and the still-pending independent delta re-audit.

## Finding dispositions

| ID | Severity | Accepted | Exact correction | Affected files or artifacts | Validation required for closure | Blocking stage | Residual limitation after correction |
|---|---|---|---|---|---|---|---|
| REAUDIT-PN-001 | HIGH | `accepted` | Give every one of the 14 mandatory mutations a stable, case-specific expected error code. Make the validator emit structured codes and make the negative runner require `actual_error_code == expected_error_code`; a generic earlier regeneration or aggregate guard must not satisfy the case. Preserve the aggregate fail-closed assertion as an additional check. | `tests/project-normalization-synthesis-v1-1-negative.mjs`; `scripts/validate-project-normalization-synthesis-v1-1.mjs`; generated mandatory-mutation catalog and result/attestation artifacts | On the reconciled exact SHA, 14/14 cases must both exit nonzero and match their named diagnostic; mutate or remove each targeted assertion to demonstrate that the corresponding case fails for that branch rather than a generic predecessor. Publish command, exit-code and result artifacts. | exact-head independent re-audit and merge authorization | Stable diagnostics establish branch reachability, not component readiness, target-contract closure, or merge safety. A separate delta re-audit must still evaluate the new head. |
| REAUDIT-PN-002 | MEDIUM | `accepted` | Treat positive baselines as positive tests, not negative semantic mutations. Generate lane and aggregate mutation counts from the executable catalog/results, with the audited correction `90` lane negatives and `14 + 90 = 104` total negatives for the v1.1 corpus. Remove literal execution `PASS` values and literal mutation totals from the committed receipt; bind definitions/hashes there and record actual outcomes in a run attestation. | lane test runners; generated mutation catalog/results; `scripts/build-project-normalization-synthesis-v1-1-receipt.mjs`; `receipts/normalization/project-normalization-synthesis-v1-1.json`; execution attestation | A clean replay must derive the negative count, reject any catalog/result cardinality mismatch, prove the positive baseline is excluded, and make the committed receipt reproducible without self-asserted runtime success. | receipt correctness and independent audit closure | A derived attestation is evidence only for its pinned source SHA and recorded environment. It does not transfer across reconciliation and is not an independent verdict. |
| REAUDIT-PN-003 | MEDIUM | `accepted` | Add a clean exact-SHA secondary replay that records the exact commit, all commands, `node --version`, `python3 --version`, exit codes, artifact hashes and final `git status --porcelain` for both the design-system and pinned read-only product checkout. Separate the committed-range diff gate from explicitly named audit-byte exceptions, each protected by size and SHA-256. | `.github/workflows/project-normalization-synthesis-v1-1.yml`; exact-head replay/workflow helpers and their contracts/tests; replay attestation and logs | From a newly materialized clean checkout, replay every positive validator, Draft 2020-12 schemas, immutable evidence, Git census, 134/134 archive entries, secret scan and committed-range diff gate; both final worktree statuses must be empty and the recorded versions/commands/exits must be machine-checkable. | reproducibility closure before merge authorization | CI provenance and a clean replay reduce the reproducibility gap but do not constitute the independent delta re-audit. The two byte-preserved audit inputs remain deliberate, individually verified diff exceptions rather than rewritten bytes. |
| REAUDIT-PN-004 | LOW | `accepted` | Treat the prior checklist as historical evidence for reviewed head `e005a1c3fa5ffda07a8e76d994aa1d96b53ec45b`, not evidence for `bcdff9de…` or the future reconciled head. Supersede its stale 72-output statement with exact-head, CI-bound evidence derived after final receipt generation; do not let a checklist approve itself. | `.codex/integration/project-normalization-synthesis-v1-1/CHECKLIST_REVIEW.md` as historical input; v1.1.1 integration report, proof-closure report and final read-only checklist | The final checklist must name the reconciled SHA, derive the current output inventory/count from the final receipt, reference completed CI artifacts, and be authored only after integration is frozen. | exact-head provenance; not a standalone semantic blocker | A corrected implementation checklist is still not an independent red-team verdict and cannot close any remaining semantic or merge-readiness finding by summary. |
| MERGE-PN-001 | BLOCKER | `accepted` | Reconcile current `main` by a non-rewriting merge that preserves `bcdff9de…` as an ancestor. Resolve authority/Resource Graph conflicts semantically, regenerate all derived artifacts and receipts, and treat the resulting commit as a new unaudited candidate. Never mechanically choose one conflict side or force-push the audited line. | Git ancestry and conflict resolutions; affected authority/Resource Graph contracts and documentation; all regenerated normalization outputs, receipt inventory and attestations | Prove audited-head ancestry, record merge base/main/reconciled SHAs, run the full v1.1.1 local and CI matrix on the reconciled SHA, obtain clean status, and complete an independent delta re-audit over `bcdff9de… → reconciled head` plus the incorporated `main` delta. | merge; no merge authorization before the rerun and delta re-audit | Reconciliation necessarily invalidates automatic transfer of the exact-head audit. Even green reruns leave merge blocked until the new candidate receives the independent delta verdict. |
| MERGE-PN-002 | MEDIUM | `accepted` | Cover both omitted contract inputs—`contracts/page-archetype-requirements.v1.json` and `contracts/resource-graph-004.plugin.json`—in automatic workflow triggers. Maintain and validate an authoritative input-path registry so a future contract input cannot silently escape the workflow filter. | `.github/workflows/project-normalization-synthesis-v1-1.yml`; workflow input-path registry/schema or contract; filter-consistency validator and negative tests | Prove each required input is represented by the workflow filter, prove either omitted contract makes validation fail, and show a change limited to each contract schedules the v1.1.1 validation workflow. | automated coverage for reconciliation/delta and merge authorization | Trigger coverage proves that validation is scheduled, not that validation is correct or successful; merge still depends on the pinned run artifacts and independent delta re-audit. |

## Decision boundary

These dispositions authorize proof remediation only. They do not authorize a
production UI, Penpot, prototype, token, component, product-model, experiment,
normalization, migration, deletion, deprecation, promotion, or merge decision.
The source verdict remains **`FAIL — SEMANTIC_REMEDIATION_PARTIALLY_PROVEN`** and
**`BLOCKED — MERGE_NOT_AUTHORIZED`** until evidence from the new exact head is
independently re-audited.
