#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';
import { fileURLToPath } from 'node:url';

const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');
const EXPECTATIONS='catalog/penpot-executor/g11/exact-expectations.json';
const OUT='catalog/penpot-executor/g11/capsule/penpot-visual-executor.g11.js';
const MANIFEST='catalog/penpot-executor/g11/capsule/manifest.json';
const read=p=>fs.readFileSync(path.join(ROOT,p));
const sha=b=>crypto.createHash('sha256').update(b).digest('hex');
const parse=p=>JSON.parse(read(p));
const stable=v=>JSON.stringify(v,(_k,x)=>x&&typeof x==='object'&&!Array.isArray(x)?Object.fromEntries(Object.entries(x).sort(([a],[b])=>a.localeCompare(b))):x);
const exp=parse(EXPECTATIONS);
if(exp.schema!=='kenigevents.penpot-executor-independent-expectations.v1')throw Error('EXPECTATIONS_SCHEMA');
const page=parse('catalog/ui-conformance/free-collection/g4/resolved/free-collection.desktop.full.resolved-render-case.json');
const fixtures=Object.fromEntries(page.payload.fixtures.map(x=>[x.fixture_id,x.preview_event]));
for(const c of exp.cases){const f=fixtures[c.fixture_id];if(!f)throw Error(`FIXTURE_MISSING:${c.fixture_id}`);if(f.title!==c.content.title)throw Error(`TITLE_MISMATCH:${c.fixture_id}`);if([f.city,f.venue_name].filter(Boolean).join(' · ')!==c.content.place)throw Error(`PLACE_MISMATCH:${c.fixture_id}`);}
const bytes={};
for(const a of [...exp.assets,...exp.media_assets]){const b=read(a.path);if(sha(b)!==a.sha256)throw Error(`ASSET_HASH_MISMATCH:${a.path}`);bytes[a.id||a.fixture_id]={base64:b.toString('base64'),sha256:a.sha256,path:a.path};}
const sourcePaths=[EXPECTATIONS,'catalog/ui-conformance/free-collection/g4/resolved/resolved-cases.index.json','catalog/ui-conformance/free-collection/g4/resolved/geometry-proof.json','catalog/ui-conformance/free-collection/g4/evidence-plan.json','catalog/ui-components/event-card-large/component-contract.v2.json','catalog/fixtures/design-system-reference/v2/scenarios/archetype.collections.free.september.desktop-ready.v3.json','catalog/fixtures/design-system-reference/v2/scenarios/archetype.collections.free.september.mobile-ready.v3.json'];
const payload={schema:'kenigevents.penpot-visual-executor-payload.v2',semantic_identity:'kenigevents.free-collection.ordinary-penpot-visual-executor',revision:'run-agnostic-v2',expectations:exp,bytes,inputs:sourcePaths.map(path=>({path,sha256:sha(read(path))}))};
payload.content_sha256=sha(Buffer.from(stable(payload)));

function runtimeSource(data){return `/* GENERATED, RUN-AGNOSTIC, ORDINARY-PENPOT ONLY. No run authority is embedded. */
(async()=>{'use strict';
const PAYLOAD=${JSON.stringify(data)};
const REQUIRED_PENPOT=['createBoard','createRectangle','createText','createShapeFromSvg','uploadMediaData'];
const REQUIRED_UTILS=['findShape','setParentXY'];
function fail(code,details){const e=new Error(code);e.code=code;e.details=details||null;throw e;}
function stable(v){if(Array.isArray(v))return '['+v.map(stable).join(',')+']';if(v&&typeof v==='object')return '{'+Object.keys(v).sort().map(k=>JSON.stringify(k)+':'+stable(v[k])).join(',')+'}';return JSON.stringify(v);}
function sha256(ascii){function rr(v,n){return(v>>>n)|(v<<(32-n));}const max=Math.pow(2,32),words=[],len=ascii.length*8;let h=sha256.h=sha256.h||[],k=sha256.k=sha256.k||[],pc=k.length;for(let n=2;pc<64;n++){let prime=true;for(let f=2;f*f<=n;f++)if(n%f===0){prime=false;break;}if(prime){if(pc<8)h[pc]=(Math.pow(n,.5)*max)|0;k[pc++]=(Math.pow(n,1/3)*max)|0;}}ascii+='\\x80';while(ascii.length%64-56)ascii+='\\x00';for(let i=0;i<ascii.length;i++){const j=ascii.charCodeAt(i);if(j>>8)fail('NON_ASCII_HASH_INPUT');words[i>>2]|=j<<((3-i)%4)*8;}words[words.length]=len/max|0;words[words.length]=len;for(let j=0;j<words.length;){const w=words.slice(j,j+=16),old=h.slice(0);for(let i=0;i<64;i++){const w15=w[i-15],w2=w[i-2],a=h[0],e=h[4];const t1=h[7]+(rr(e,6)^rr(e,11)^rr(e,25))+((e&h[5])^((~e)&h[6]))+k[i]+(w[i]=i<16?w[i]:(w[i-16]+(rr(w15,7)^rr(w15,18)^(w15>>>3))+w[i-7]+(rr(w2,17)^rr(w2,19)^(w2>>>10)))|0);const t2=(rr(a,2)^rr(a,13)^rr(a,22))+((a&h[1])^(a&h[2])^(h[1]&h[2]));h=[(t1+t2)|0,a,h[1],h[2],(h[3]+t1)|0,e,h[5],h[6]];}for(let i=0;i<8;i++)h[i]=(h[i]+old[i])|0;}return h.map(x=>(x>>>0).toString(16).padStart(8,'0')).join('');}
function utf8(s){return unescape(encodeURIComponent(s));}
function digest(v){return sha256(utf8(stable(v)));}
function without(o,keys){const x={};for(const k of Object.keys(o))if(!keys.includes(k))x[k]=o[k];return x;}
function semantic(shape){try{return shape.getPluginData('kenigevents-semantic-key')||'';}catch{return '';}}
function exactContext(caseId){return PAYLOAD.expectations.cases.find(x=>x.case_id===caseId);}
function caseForFixture(fixture,viewport,groupId){const packing=groupId.includes('exhibitions')?'packed':'wide';const base=PAYLOAD.expectations.cases.find(x=>x.variant.startsWith(viewport+'-'+packing));if(!base)fail('CONTEXT_EXPECTATION_MISSING',{fixture,viewport,groupId});return Object.assign({},base,{fixture_id:fixture,content:PAYLOAD.expectations.fixture_content[fixture]});}
function buildEntities(){const E=[],root='g11.evidence.root';E.push({key:root,type:'board',parent:null,name:'KenigEvents · bounded L0–L3 proof',box:[0,0,2680,4400],props:{fills:[{fillColor:'#fffdf8',fillOpacity:1}],clipContent:false}});const l0='g11.L0';E.push({key:l0,type:'board',parent:root,name:'L0 · exact assets',box:[24,24,1240,620],layout:{dir:'row',wrap:'wrap',gap:16,padding:16}});let ax=0,ay=0;for(const a of PAYLOAD.expectations.assets){E.push({key:'asset.'+a.id,type:'svg',parent:l0,name:a.id,box:[ax,ay,96,96],asset:a.id});ax+=112;if(ax>1000){ax=0;ay+=112;}}for(const m of PAYLOAD.expectations.media_assets){E.push({key:'media.'+m.fixture_id,type:'media',parent:l0,name:m.fixture_id,box:[ax,ay,144,112],asset:m.fixture_id});ax+=160;if(ax>1000){ax=0;ay+=128;}}
const l1='g11.L1';E.push({key:l1,type:'board',parent:root,name:'L1 · leaf owners',box:[24,680,1240,340],layout:{dir:'row',wrap:'wrap',gap:16,padding:16}});for(const [i,name] of ['media','event-type','admission','actions','social-proof','medallion'].entries())E.push({key:'leaf.'+name,type:'board',parent:l1,name:'leaf · '+name,box:[i*196,0,180,130],props:{fills:[{fillColor:'#15110f',fillOpacity:1}],borderRadius:20}});
const masterCase=exactContext('eventcard.desktop-wide-calendar.8006'),master='component.event-card.large';E.push({key:master,type:'board',parent:root,name:'EventCard · Large · governed master',box:[1320,24,masterCase.box.width,masterCase.box.height],props:{fills:[{fillColor:'#15110f',fillOpacity:1}],borderRadius:24,clipContent:true},componentMaster:true,plugin:{'kenigevents-component-identity':PAYLOAD.expectations.component_model.semantic_identity,'kenigevents-variant-axes':PAYLOAD.expectations.component_model.variant_axes.join(',')}});E.push({key:master+'.media',type:'media',parent:master,name:'media',box:[masterCase.media_frame.x,masterCase.media_frame.y,masterCase.media_frame.width,masterCase.media_frame.height],asset:masterCase.fixture_id,props:{clipContent:true,borderRadius:24}});for(const slot of ['title','occurrence','place','event-type','admission','shares','likes'])E.push({key:master+'.'+slot,type:'text',parent:master,name:slot,box:slot==='title'?[...Object.values(masterCase.title_box)]:[14.6,690+(['occurrence','place'].indexOf(slot)*22),500,22],text:String(masterCase.content[slot.replace('-','_')]??''),style:slot==='title'?masterCase.typography.title:masterCase.typography.meta});for(const [i,a] of ['share','like','not_interested','calendar'].entries())E.push({key:master+'.action.'+a,type:'svg',parent:master,name:a,box:[14.6+i*52,805,44,44],asset:{share:'action-share',like:'action-favorite-outline',not_interested:'action-not-interested',calendar:'action-calendar-add'}[a]});
const l2='g11.L2';E.push({key:l2,type:'board',parent:root,name:'L2 · four exact EventCard contexts',box:[24,1060,1240,1500],layout:{dir:'row',wrap:'wrap',gap:20,padding:16}});for(const [i,c] of PAYLOAD.expectations.cases.entries())E.push({key:'case.'+c.case_id,type:'instance',parent:l2,name:c.case_id,box:[(i%2)*570,Math.floor(i/2)*760,c.box.width,c.box.height],instanceOf:master,caseId:c.case_id,plugin:{'kenigevents-variant':c.variant,'kenigevents-fixture':c.fixture_id}});
const l3='g11.L3';E.push({key:l3,type:'board',parent:root,name:'L3 · rows and groups',box:[24,2600,2500,1700],layout:{dir:'column',gap:24,padding:16}});let gy=0;for(const g of PAYLOAD.expectations.groups){const gw=g.viewport==='desktop'?1120:372;E.push({key:'group.'+g.group_id,type:'board',parent:l3,name:g.group_id,box:[g.viewport==='desktop'?0:1180,gy,gw,g.row_height+90],layout:{dir:g.viewport==='desktop'?'row':'column',gap:g.gap,padding:12,alignItems:'stretch'},plugin:{'kenigevents-equal-height':String(g.equal_height)}});E.push({key:'heading.'+g.group_id,type:'text',parent:'group.'+g.group_id,name:'heading',box:[0,0,gw-24,48],text:g.heading,style:{fontFamily:'Inter',fontSize:38.4,fontWeight:900,lineHeight:1.08,fill:'#221a14'}});for(const [i,f] of g.fixtures.entries()){const c=caseForFixture(f,g.viewport,g.group_id);E.push({key:'group.'+g.group_id+'.'+f,type:'instance',parent:'group.'+g.group_id,name:f,box:[g.viewport==='desktop'?i*(g.card_width+g.gap):0,56+(g.viewport==='mobile'?i*(g.row_height+g.gap):0),g.card_width,g.row_height],instanceOf:master,caseId:c.case_id,fixtureId:f,plugin:{'kenigevents-group':g.group_id,'kenigevents-fixture':f,'kenigevents-equal-height-row':String(g.equal_height)}});}gy+=g.row_height+130;}
return E.map(e=>Object.assign({},e,{props:Object.assign({name:e.name},e.props||{})}));}
function operationPlan(){const ops=[];let componentPlanned=false;for(const e of buildEntities()){if(e.type==='instance'&&!componentPlanned){ops.push('component-create:'+e.instanceOf);componentPlanned=true;}ops.push('create:'+e.key,'resize:'+e.key,'position:'+e.key);if(e.type==='media')ops.push('set:'+e.key+':fills');for(const k of Object.keys(e.props||{}).sort())ops.push('set:'+e.key+':'+k);for(const k of Object.keys(e.style||{}).sort())ops.push('style:'+e.key+':'+k);for(const k of Object.keys(e.plugin||{}).sort())ops.push('plugin:'+e.key+':'+k);ops.push('plugin:'+e.key+':kenigevents-semantic-key','plugin:'+e.key+':kenigevents-payload-sha256','append:'+e.key);if(e.layout){ops.push('layout-create:'+e.key);for(const k of Object.keys(e.layout).sort())ops.push('layout-set:'+e.key+':'+k);}if(e.type==='instance'&&e.caseId){for(const slot of ['title','occurrence','place','event-type','admission','shares','likes'])ops.push('override:'+e.key+':'+slot);ops.push('override:'+e.key+':actions');}}return ops;}
function rollbackPlan(){return buildEntities().map(e=>'rollback:'+e.key).reverse();}
function validatePrimitives(){if(typeof penpot!=='object'||!penpot)fail('PENPOT_GLOBAL_MISSING');if(typeof penpotUtils!=='object'||!penpotUtils)fail('PENPOT_UTILS_GLOBAL_MISSING');for(const k of REQUIRED_PENPOT)if(typeof penpot[k]!=='function')fail('PENPOT_PRIMITIVE_MISSING',{key:k});for(const k of REQUIRED_UTILS)if(typeof penpotUtils[k]!=='function')fail('PENPOT_UTIL_PRIMITIVE_MISSING',{key:k});if(!penpot.library?.local||typeof penpot.library.local.createComponent!=='function')fail('PENPOT_COMPONENT_API_MISSING');}
function validateAuthority(capsule,probe){if(!capsule||capsule.schema!=='kenigevents.w0-issued-w3-penpot-run-capsule.v1')fail('RUN_CAPSULE_MISSING');if(capsule.owner!=='W3')fail('RUN_OWNER_NOT_W3');if(!Number.isInteger(capsule.generation)||capsule.generation<1)fail('RUN_GENERATION_INVALID');if(!capsule.run_id||!capsule.file_id||!capsule.page_id||!capsule.nonce)fail('RUN_BINDING_INCOMPLETE');if(Date.parse(capsule.expires_at)<=Date.now())fail('RUN_CAPSULE_EXPIRED');if(capsule.cancelled!==false)fail('RUN_CANCELLED');if(capsule.accepted_executor?.semantic_identity!==PAYLOAD.semantic_identity||capsule.accepted_executor?.payload_sha256!==PAYLOAD.content_sha256)fail('EXECUTOR_TUPLE_MISMATCH');if(digest(without(capsule,['tickets','w0_binding']))!==capsule.w0_binding?.body_sha256||!String(capsule.w0_binding?.signature||'').startsWith('W0:'))fail('W0_CAPSULE_BINDING_INVALID');const roots=(capsule.tickets||[]).map(t=>t.body_sha256);if(digest(roots)!==capsule.ticket_root_sha256)fail('TICKET_ROOT_MISMATCH');const rollbackRoots=(capsule.rollback_tickets||[]).map(t=>t.body_sha256);if(digest(rollbackRoots)!==capsule.rollback_ticket_root_sha256)fail('ROLLBACK_TICKET_ROOT_MISMATCH');if(!probe||probe.schema!=='kenigevents.penpot-native-compatibility-probe-receipt.v1'||probe.status!=='COMPATIBLE')fail('COMPATIBILITY_PROBE_REQUIRED');if(probe.file_id!==capsule.file_id||probe.page_id!==capsule.page_id||probe.strategy!=='clipped-native-image-shape-v1')fail('COMPATIBILITY_PROBE_MISMATCH');if(digest(without(probe,['w0_binding']))!==probe.w0_binding?.body_sha256||!String(probe.w0_binding?.signature||'').startsWith('W0:'))fail('COMPATIBILITY_PROBE_BINDING_INVALID');return capsule;}
async function execute({capsule,probeReceipt}={}){validatePrimitives();validateAuthority(capsule,probeReceipt);const receipt={schema:'kenigevents.penpot-visual-executor-receipt.v2',executor:{semantic_identity:PAYLOAD.semantic_identity,payload_sha256:PAYLOAD.content_sha256},run:{owner:capsule.owner,run_id:capsule.run_id,generation:capsule.generation,file_id:capsule.file_id,page_id:capsule.page_id,nonce:capsule.nonce},input_hashes:PAYLOAD.inputs,operations:[],pre_write_checks:[],consumed_tickets:[],created:[],reused:[],component_lineage:[],completed_mutations:[],rolled_back_mutations:[],unreverted_mutations:[],terminal_state:'PREFLIGHT'};const used=new Set(),created=[],shapes=new Map(),components=new Map();let ticketIndex=0,rollbackTicketIndex=0;
function ticket(op){const t=capsule.tickets[ticketIndex++];if(!t)fail('MUTATION_TICKET_MISSING',{op});if(used.has(t.nonce))fail('MUTATION_TICKET_REPLAY',{op,nonce:t.nonce});if(t.schema!=='kenigevents.w0-penpot-mutation-ticket.v1'||t.owner!=='W3'||t.run_id!==capsule.run_id||t.generation!==capsule.generation||t.file_id!==capsule.file_id||t.page_id!==capsule.page_id||t.operation!==op||t.sequence!==ticketIndex-1)fail('MUTATION_TICKET_BINDING_MISMATCH',{op,ticket:t});if(t.cancelled!==false)fail('MUTATION_TICKET_CANCELLED',{op});if(Date.parse(t.expires_at)<=Date.now())fail('MUTATION_TICKET_EXPIRED',{op});if(digest(without(t,['body_sha256','w0_signature']))!==t.body_sha256||!String(t.w0_signature||'').startsWith('W0:'))fail('MUTATION_TICKET_SIGNATURE_BINDING_INVALID',{op});used.add(t.nonce);receipt.pre_write_checks.push({operation:op,sequence:t.sequence,generation:t.generation,nonce:t.nonce,cancelled:t.cancelled,checked_immediately_before_write:true});receipt.consumed_tickets.push({operation:op,nonce:t.nonce,body_sha256:t.body_sha256});return t;}
async function write(op,fn){ticket(op);const v=await fn();receipt.operations.push({sequence:receipt.operations.length,operation:op,status:'COMPLETED'});receipt.completed_mutations.push(op);return v;}
function rollbackTicket(op){const t=(capsule.rollback_tickets||[]).find(x=>x.operation===op);rollbackTicketIndex++;if(!t)fail('ROLLBACK_TICKET_MISSING',{op});if(used.has(t.nonce))fail('MUTATION_TICKET_REPLAY',{op,nonce:t.nonce});if(t.owner!=='W3'||t.run_id!==capsule.run_id||t.generation!==capsule.generation||t.file_id!==capsule.file_id||t.page_id!==capsule.page_id||t.operation!==op||t.cancelled!==false||Date.parse(t.expires_at)<=Date.now()||digest(without(t,['body_sha256','w0_signature']))!==t.body_sha256||!String(t.w0_signature||'').startsWith('W0:'))fail('ROLLBACK_TICKET_INVALID',{op});used.add(t.nonce);receipt.pre_write_checks.push({operation:op,sequence:t.sequence,generation:t.generation,nonce:t.nonce,cancelled:t.cancelled,checked_immediately_before_write:true,rollback:true});receipt.consumed_tickets.push({operation:op,nonce:t.nonce,body_sha256:t.body_sha256});}
function find(key){return penpotUtils.findShape(s=>semantic(s)===key)||null;}
function descendant(root,suffix){const target=semantic(root)+'.'+suffix;const stack=[...(root.children||[])];while(stack.length){const s=stack.shift();if(semantic(s)===target||semantic(s).endsWith('.'+suffix)||s.name===suffix)return s;stack.push(...(s.children||[]));}return null;}
try{const existing=find('g11.evidence.root');if(existing){receipt.reused.push({semantic_key:'g11.evidence.root',id:existing.id});receipt.terminal_state='SUCCEEDED_IDEMPOTENT_REUSE';return receipt;}for(const e of buildEntities()){let shape;if(e.type==='board')shape=await write('create:'+e.key,()=>penpot.createBoard());else if(e.type==='text')shape=await write('create:'+e.key,()=>penpot.createText(e.text));else if(e.type==='svg')shape=await write('create:'+e.key,()=>penpot.createShapeFromSvg(PAYLOAD.bytes[e.asset].base64?atob(PAYLOAD.bytes[e.asset].base64):PAYLOAD.bytes[e.asset]));else if(e.type==='media'){const data=Uint8Array.from(atob(PAYLOAD.bytes[e.asset].base64),c=>c.charCodeAt(0));const image=await write('create:'+e.key,()=>penpot.uploadMediaData(e.name,data,'image/webp'));shape=await write('resize:'+e.key,()=>{const s=penpot.createRectangle();s.__uploaded=image;return s;});}else if(e.type==='instance'){if(!components.has(e.instanceOf)){const masterShape=shapes.get(e.instanceOf);const createdComponent=await write('component-create:'+e.instanceOf,()=>penpot.library.local.createComponent([masterShape]));components.set(e.instanceOf,createdComponent);receipt.component_lineage.push({semantic_identity:PAYLOAD.expectations.component_model.semantic_identity,master_id:masterShape.id,component_id:createdComponent.id,variant_axes:PAYLOAD.expectations.component_model.variant_axes});}const comp=components.get(e.instanceOf);if(!comp)fail('MASTER_COMPONENT_MISSING',{key:e.key});shape=await write('create:'+e.key,()=>comp.instance());}else shape=await write('create:'+e.key,()=>penpot.createRectangle());if(e.type!=='media')await write('resize:'+e.key,()=>shape.resize(e.box[2],e.box[3]));else{await write('position:'+e.key,()=>shape.resize(e.box[2],e.box[3]));shape.fills=[{fillImage:shape.__uploaded,fillOpacity:1}];}if(e.type!=='media')await write('position:'+e.key,()=>penpotUtils.setParentXY(shape,e.box[0],e.box[1]));else await write('set:'+e.key+':fills',()=>shape.fills=[{fillImage:shape.__uploaded,fillOpacity:1}]);for(const k of Object.keys(e.props||{}).sort())await write('set:'+e.key+':'+k,()=>shape[k]=e.props[k]);for(const k of Object.keys(e.style||{}).sort())await write('style:'+e.key+':'+k,()=>shape[k]=e.style[k]);for(const k of Object.keys(e.plugin||{}).sort())await write('plugin:'+e.key+':'+k,()=>shape.setPluginData(k,String(e.plugin[k])));await write('plugin:'+e.key+':kenigevents-semantic-key',()=>shape.setPluginData('kenigevents-semantic-key',e.key));await write('plugin:'+e.key+':kenigevents-payload-sha256',()=>shape.setPluginData('kenigevents-payload-sha256',PAYLOAD.content_sha256));const parent=e.parent?shapes.get(e.parent):penpot.currentPage.root;await write('append:'+e.key,()=>parent.appendChild(shape));if(e.layout){const layout=await write('layout-create:'+e.key,()=>shape.addFlexLayout());for(const k of Object.keys(e.layout).sort())await write('layout-set:'+e.key+':'+k,()=>layout[k]=e.layout[k]);}if(e.type==='instance'&&e.caseId){const c=exactContext(e.caseId);for(const slot of ['title','occurrence','place','event-type','admission','shares','likes']){const n=descendant(shape,slot);if(!n)fail('INSTANCE_SLOT_MISSING:'+e.key+':'+slot,{key:e.key,slot});const val=c.content[slot.replace('-','_')]??'';await write('override:'+e.key+':'+slot,()=>n.characters=String(val));}await write('override:'+e.key+':actions',()=>shape.setPluginData('kenigevents-actions',c.actions.join(',')));receipt.component_lineage.push({instance_id:shape.id,semantic_key:e.key,component_id:components.get(e.instanceOf).id,case_id:e.caseId,fixture_id:e.fixtureId||c.fixture_id,linked:true});}shapes.set(e.key,shape);created.push(shape);receipt.created.push({semantic_key:e.key,id:shape.id,type:e.type});}receipt.terminal_state='SUCCEEDED';return receipt;}catch(error){const completed=[...receipt.completed_mutations];for(const shape of created.reverse()){try{if(typeof shape.remove!=='function')throw Error('REMOVE_UNAVAILABLE');rollbackTicket('rollback:'+semantic(shape));shape.remove();receipt.rolled_back_mutations.push({id:shape.id,semantic_key:semantic(shape)});}catch(e){receipt.unreverted_mutations.push({id:shape.id,semantic_key:semantic(shape),error:String(e.message||e)});}}receipt.terminal_state=receipt.unreverted_mutations.length?'FAILED_PARTIAL_STATE':'FAILED_ROLLED_BACK';receipt.failure={code:error.code||'EXECUTION_FAILED',message:String(error.message||error),completed};error.receipt=receipt;throw error;}}
const api=Object.freeze({identity:PAYLOAD.semantic_identity,payload_sha256:PAYLOAD.content_sha256,operationPlan,rollbackPlan,execute,threat_model:{authority:'W0-issued W3 capsule plus one hash-bound non-replayable ticket per native write',limitation:'Offline ordinary-Penpot execution cannot observe a cancellation issued after W0 minted the final ticket; W0 must issue bounded short-lived tickets and stop issuance on cancel.',operator_tamper_resistance:'Hash bindings detect accidental or stale data; operator-controlled runtime is outside this bounded trust model.'}});globalThis.__KENIGEVENTS_RUN_AGNOSTIC_EXECUTOR__=api;return api;})()`;}

const source=runtimeSource(payload)+'\n';
fs.mkdirSync(path.dirname(path.join(ROOT,OUT)),{recursive:true});fs.writeFileSync(path.join(ROOT,OUT),source);
const manifest={schema:'kenigevents.penpot-visual-executor-manifest.v2',semantic_identity:payload.semantic_identity,revision:payload.revision,run_agnostic:true,executor:{path:OUT,raw_sha256:sha(Buffer.from(source)),content_sha256:sha(Buffer.from(source.replace(/\r\n/g,'\n')))},payload_sha256:payload.content_sha256,expectations:{path:EXPECTATIONS,sha256:sha(read(EXPECTATIONS))},supported_scope:{levels:['L0_ASSETS','L1_LEAVES','L2_EVENTCARD','L3_ROWS_AND_GROUPS'],case_ids:exp.cases.map(x=>x.case_id),group_ids:exp.groups.map(x=>x.group_id),asset_ids:exp.assets.map(x=>x.id),media_fixture_ids:exp.media_assets.map(x=>x.fixture_id)},required_primitives:{penpot:['createBoard','createRectangle','createText','createShapeFromSvg','uploadMediaData','library.local.createComponent'],penpotUtils:['findShape','setParentXY']},authorization:{embedded_runtime_authority:false,required_capsule_schema:'contracts/penpot-executor/w0-issued-w3-run-capsule.v1.schema.json',required_ticket_schema:'contracts/penpot-executor/w0-penpot-mutation-ticket.v1.schema.json',required_probe_receipt_schema:'contracts/penpot-executor/penpot-native-compatibility-probe-receipt.v1.schema.json'},supersedes:{generation:10,commit:'fed36b2839336f38bfc459ce3ed1e681a46ad9e8',artifact_raw_sha256:'b5065b857b3218625897cf02adecb29a9bded4c214a0191f32fd9a6aed384ea7'},deterministic:true,penpot_mutation_performed:false};
fs.writeFileSync(path.join(ROOT,MANIFEST),JSON.stringify(manifest,null,2)+'\n');
console.log(JSON.stringify({out:OUT,manifest:MANIFEST,payload_sha256:payload.content_sha256,raw_sha256:manifest.executor.raw_sha256}));
