#!/usr/bin/env python3
import hashlib,json
from pathlib import Path
ROOT=Path(__file__).resolve().parents[1];CASE=ROOT/'catalog/fixtures/ui-reference-events/v1/cases/event-card-large-portrait-poster-8156-mobile-390.case.json';RESOLVED=ROOT/'catalog/fixtures/ui-reference-events/v1/cases/resolved/event-card-large-portrait-poster-8156-mobile-390.resolved-render-case.json';RECEIPT=ROOT/'receipts/penpot/event-card-large-mobile-390-portrait-materialization-v1.json';TELEGRAM=ROOT/'receipts/ui-conformance/incremental-event-components-20260822/telegram/event-card-large-8156-mobile-390.json'
def stable(v):p=dict(v);p.pop('resolved_render_case_sha256',None);return hashlib.sha256((json.dumps(p,sort_keys=True,separators=(',',':'),ensure_ascii=False)+'\n').encode()).hexdigest()
case=json.loads(CASE.read_text());resolved=json.loads(RESOLVED.read_text());receipt=json.loads(RECEIPT.read_text());telegram=json.loads(TELEGRAM.read_text())
assert resolved['resolved_render_case_sha256']==stable(resolved)==case['resolved_render_case_sha256'];g=resolved['resolved_media']['frame_geometry'];assert g['content_width']==388 and g['content_height']==582 and g['intrinsic_width']==1706 and g['intrinsic_height']==2559;assert resolved['resolved_media']['fit']=='contain';assert resolved['resolved_visibility']['calendar'] is False;assert case['penpot_binding']['binding_status']=='bound';assert case['penpot_binding']['board_or_component_id']==receipt['review_board_id'];assert case['penpot_binding']['export_sha256']==receipt['comparison']['penpot_export_sha256'];assert receipt['penpot_readback']['calendar_shapes']==0;assert receipt['penpot_readback']['detached_nested_boards']==0;assert receipt['penpot_readback']['terminal_card_override'] is False;assert receipt['penpot_readback']['penpot_validation_errors']==0;assert receipt['overall_card_family_status']=='unfinished';assert telegram['message_id']==1073 and telegram['read_back_status']=='verified' and telegram['telegram_media_metadata']['caption_exact'] is True
print(f'PASS {CASE.relative_to(ROOT)} {resolved["resolved_render_case_sha256"]}')
