# Behavioral Decoder v1.1 design-repository import

## Imported evidence

- Imported the reviewed behavioral v1.1 compact supplement as a sibling of immutable Decoder v1.
- Added a fail-closed behavioral supplement validator and a separate GitHub Actions workflow.
- Kept the existing v1 validator unchanged and pinned the immutable v1 Git tree to
  `e77fc2457fadfdffb46ed2d90304ebb91e89a715` in the new workflow.
- Updated catalog/navigation/source-first documentation and the R-07 living adoption ledger with exact provenance.
- Corrected the reviewed v1 raster count from 155 to 157.
- Did not edit R-01…R-06 and did not rewrite immutable Decoder v1.

## Receipt

- Imported supplement commit: `7da6cfb18763e34a81b4890249e490e0945e9a07`.
- Imported supplement tree: `b558869473397a51d03e4f87220e6a1889a088ea`.
- Manifest SHA-256: `c6c62cee8bea4e9440ff85bc75c46bc85cf5abf3e2fdcd4c7357c6ece916436f`.
- Actions run: `31318132051`; artifact `9039433060`; archive SHA-256
  `c677f69572ccdbf5b7f1402037a3cb8c164bd2f503fae35eae9168c46eb8d909`.
- Full-resolution visual review: 124/124; ledger SHA-256
  `97c8cbcf2e4bbc34fd7e8c03454f09219bfb723acd4751b89744d6a8eb0f7731`.
- Independent audit: `PASS` for truthful evidence import.
- Final status: `EVIDENCE_COLLECTION_INCOMPLETE` because exactly two readiness blockers remain.

Both validators and the immutable v1 tree assertion pass. The result is durable evidence, not
permission to normalize, consolidate components, choose a CTA/transport winner or mutate Penpot.

## Project Normalization Synthesis v1.1 — R11/R12 evidence/value remediation

This lane is an evidence and observe-mode gate hardening only. It does not edit the Decoder v1 or
Behavioral v1.1 source packages, events-bot-new, production UI, Penpot, tokens, component decisions,
or product authority.

### R11 — row-level visual lineage and canonical counts

- `catalog/normalization/visual-review-evidence.jsonl` contains exactly 134 unique raster/review/state
  rows joined one-to-one to the behavioral observation, page-verification, visual-review, and image
  index sets. Every row records a canonical raster ID, SHA-256, bytes, archive entry, permanent GitHub
  Release asset, reviewer/time, manual full-resolution method, conclusion, and packet/phase state.
- The 124 inherited behavior rasters point to release asset `507595606`, archive SHA-256
  `c677f69572ccdbf5b7f1402037a3cb8c164bd2f503fae35eae9168c46eb8d909`, entry prefix `capture/`.
  The 10 closure rasters point to asset `507763470`, archive SHA-256
  `8bb8712effaa0ba3b08a672a784d9e1b90d876c6ca6d039a417bfc0617723523`, entry prefix
  `probe-runtime/`. HTTP 200/content length, complete ZIP integrity, and all 134 entry bytes/SHA-256
  were verified at `2026-08-10T07:22:28Z`.
- `catalog/normalization/behavioral-manifest-counts.json` projects the sole active canonical
  snake_case count namespace from `manifest.outputs[*].records` without rewriting the source
  manifest. Five stale source-manifest conflicts (`actionPacketIndex`, `pageVerification`, `plans`,
  `unresolved`, and `visualReviewLedger`) are retained only as excluded, non-authoritative audit
  observations; the current namespace has zero conflicts and zero active legacy aliases.
- The behavioral source manifest remains SHA-256
  `c676be4f2ad956b8a58c7707c8f71b7bb33afd771e506457309597e76d67d9a1`.
- Decoder v1 does not provide a deterministic raster-to-component binding for all 134 states.
  `component_state.component_id` therefore remains truthfully null while packet, phase, observation,
  page-verification, route, viewport, probes, and a state digest provide the exact state identity.

### R12 — independent census and observe-mode product gate

- `catalog/normalization/independent-consumer-census.jsonl` is generated by a separate raw-Git-object
  import scanner. It uses only Decoder `source-files.jsonl` for the stable 107-component ID/path
  inventory and never reads `consumers.jsonl`, component `plane_bindings`, or derived consumer lists.
  The scanner replays pinned `events-bot-new` `site/src` trees `2b48fb6f…` and `d46996c4…`, records
  component/importer blob IDs and SHA-256 hashes, and yields exactly 239 unique edges from 477 plane
  occurrences (239 current-root, 238 candidate) plus exactly three zero-consumer component records.
- All pre-existing 239 application IDs and `(component_id, consumer)` keys are preserved. Each active
  row now binds exactly one census edge and a repository/component/consumer identity digest. The 239
  readiness rows mirror census, parent/effective-source, resolver, and lifecycle state one-to-one.
- `contracts/product-value-evidence-binding.v1.schema.json` now requires nullable parent/enabler,
  effective source, census, stable identity, lifecycle, deletion/successor, record-preservation, and
  adoption fields. It separates ID arrays by authoritative entity kind without inventing an ID naming
  convention. Semantic validation resolves kind against a supplied registry, rejects unknown or
  wrong-kind IDs, and keeps all arrays/claims/receipts empty while the pinned registry is absent.
- Current observe state remains 239 pending applications: 171 direct, 50 enabler, 18 experimental,
  zero inherited, zero authoritative IDs, zero promotions. Inherited mode requires an existing
  same-authority parent, forbids child overrides, resolves the effective ancestor, and rejects self or
  any-depth cycles. A missing census edge becomes preservation-on pending reconciliation; removal is
  never silent, and tombstoning requires an explicit deletion receipt while retaining the stable row.

### Deterministic validation

```sh
node scripts/build-evidence-value-gates-v1-1.mjs . --events-repo /home/dev/projects/events-bot-new
node scripts/validate-evidence-value-gates-v1-1.mjs . --events-repo /home/dev/projects/events-bot-new
node scripts/test-evidence-value-gates-v1-1-negative.mjs .
```

The build is byte-idempotent. The validator replays all 239 independent census edges from the pinned
raw Git trees. The semantic negative suite rejects 23 mutations spanning visual deletion/provenance,
stale count promotion, census drift, readiness drift, invented/wrong-kind IDs, invalid parents,
inheritance cycles, promotion flips, missing deletion receipts, silent deletion, and stable-ID drift.
It also proves the allowed preservation path for a missing edge: the stable application survives as
`pending_reconciliation` with promotion disabled until explicit reconciliation evidence exists.
Supplying both archive paths to the validator additionally replays all 134 ZIP entry SHA-256 checks.

## Project Normalization Synthesis v1.1 — integrated remediation

- The byte-preserved red-team audit is dispositioned without rewriting its
  `FAIL — RESULT_NOT_PROVEN` verdict.
- 279 raw identities map exact-once to 222 canonical findings through 57 typed
  aliases.
- 47 stable analytical groups are typed with the required nine entity kinds;
  the positive gate yields 47 `NOT_READY`, zero scores and an empty first wave.
- Event Media remains `NOT_READY_WITH_EXACT_BLOCKERS`; Event Token Medallions
  remains `BOUNDARY_AND_TAXONOMY_REVIEW_REQUIRED`.
- 134 visual records and both durable Release archives were replayed; 239
  application/readiness bindings reconcile with an independent raw-Git census.
- The Product Value gate remains observe/pending and the lifecycle remains
  `AS_IS_RECONSTRUCTED`.
- All domain validators and 14 required aggregate semantic mutations pass.

No production UI, runtime source, Penpot, token, experiment winner, product
model, component merge/split/delete or next-phase implementation is included.
