# D0 morning — F-MEDALLIONS-INSTITUTIONS-A-R2-NATIVE staged repair

- Exact parent: `f0/f-medallions-institutions-a-r2-native-20260902@03188ef9ad579ae24ffba87b6a15e65a567a4869` (`tree 72fa23b8f7a48b08097b0315cbe63c354be728da`).
- Package identity remains `F-MEDALLIONS-INSTITUTIONS-A-R2-NATIVE`; this is a package-local execution successor, not a new product package.
- Atlas R2 binding is exact: logical page `medallions-institutions-a`, physical page `04.1 · Assets · Medallions · Institutions A · Candidate`, page order `0090`, template `FOUNDATION_ASSET_GRID_DENSE_V2`, linked `ATLAS_PAGE_HEADER_V2`, and semantic slots `page_header`, `package_owned_masters`, `linked_review_instances`.
- The package-local runtime directly uses the Penpot Plugin API, awaits/settles exact page activation before geometry, and limits each authorized phase to three **actual native create calls**, not three plan records.
- The 53 managed operations resolve to exactly 63 native creates across 23 resumable phases: component masters count board + component, and WebP artworks count upload + rectangle. Native-like tests compare every receipt against the mutation-log delta.
- Every phase binds current revision, exact provider head/tree, read-only projection SHA-256 and contiguous resume cursor. Physical ACTIVE and authorization tuples bind package/session/task/writer/head/tree/trigger/lease before every operation.
- Unknown outcome forbids blind retry and requires a distinct read-only projection; terminal settlement is a separate read-only entrypoint.
- Atlas hard census is exactly 8 component masters and 24 product linked specimens. The prior 8 extra master-preview instances are absent; each component main is itself the master-column surface.
- The exact eight source descriptors and `declared-checkout.v1.json` are unchanged from the parent. Exact source files are package-local immutable runtime inputs and are rechecked against path/ref/blob/SHA-256/bytes/media type.
- Both WebP imports require `ImageData.data()` byte/SHA-256 equality and a nonempty bounded leaf PNG export before the source proof is accepted.
- Complete replay creates 0. Protected Free/EventCard and Foundations source/index projections, strict plugin-data strings, validation `[]`, detached `0`, screenshot implementations `0`, and placeholders `0` remain mandatory.
- No Penpot read or mutation, Kaggle, merge, deploy, promotion, or V0 verdict occurs in this Git-only repair.
- State remains `MAT_PACKAGE_READY_QA_INTEGRATE_GATED`; publication is not authorized until independent same-tuple QA and INTEGRATE PASS.
- Successor hardening re-reads and expiry-validates the full physical ACTIVE tuple immediately before each of all 63 native calls, including between `createBoard`/`createComponent` and awaited `uploadMediaData`/`createRectangle`; cancellation or expiry blocks the second call and returns unknown-outcome/distinct-readback evidence.
