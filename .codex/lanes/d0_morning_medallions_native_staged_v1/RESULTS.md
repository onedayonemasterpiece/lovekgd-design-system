# D0 morning — F-MEDALLIONS-INSTITUTIONS-A-R2-NATIVE staged repair

- Exact parent: `f0/f-medallions-institutions-a-r2-native-20260902@03188ef9ad579ae24ffba87b6a15e65a567a4869` (`tree 72fa23b8f7a48b08097b0315cbe63c354be728da`).
- Package identity remains `F-MEDALLIONS-INSTITUTIONS-A-R2-NATIVE`; this is a package-local execution successor, not a new product package.
- Atlas R2 binding is exact: logical page `medallions-institutions-a`, physical page `04.1 · Assets · Medallions · Institutions A · Candidate`, page order `0090`, template `FOUNDATION_ASSET_GRID_DENSE_V2`, linked `ATLAS_PAGE_HEADER_V2`, and semantic slots `page_header`, `package_owned_masters`, `linked_review_instances`.
- The package-local runtime uses the Penpot Plugin API directly, awaits/settles exact page activation before geometry, and creates at most three managed objects per authorized phase.
- Every phase is bound to current revision, exact read-only projection SHA-256 and contiguous resume cursor. ACTIVE/cancel/provenance is re-read before every create.
- Unknown outcome forbids blind retry and requires a distinct read-only projection; terminal settlement is a separate read-only entrypoint.
- Atlas hard census is exactly 8 component masters and 24 product linked specimens. The prior 8 extra master-preview instances are removed; each component main is itself the master-column surface.
- Terminal logical create census is 53 across 18 bounded phases; complete replay creates 0.
- The exact eight source descriptors and `declared-checkout.v1.json` are unchanged from the parent. SVG/WebP import requires provider-verified exact handles bound to path/ref/blob/SHA/bytes/media type.
- Protected Free/EventCard and Foundations source/index projections are checked before and after every phase. Strict plugin data strings, validation `[]`, detached `0`, screenshot implementations `0`, placeholders `0` remain mandatory.
- No Penpot read or mutation, Kaggle, merge, deploy, promotion, or V0 verdict occurs in this Git-only repair.
- State remains `MAT_PACKAGE_READY_QA_INTEGRATE_GATED`; publication is not authorized until independent same-tuple QA and INTEGRATE PASS.
