# Integration results — EventCard Large current-v2

- Base design SHA: `0882917a1328607c498d82e4c2a652bbd3df946d`
- Astro source SHA: `22ebe3c5e92b13684cca32c14357ef7b91834977`
- Events tooling SHA: `713a035a8aaa9ecfdcdd5fbd817fe504160df2f5`
- Contract SHA-256: `dfc49a54ebd9a4d2d9da1f9c24321abf349714c62d3abf1dd00f71c1ce63ae1e`
- Scope: exactly `EventCard · Large`, four desktop plus three mobile-390 cases.

## Current result

The current-v2 registry is `READY` with seven canonical IDs and no `pending-*` state. Every case joins the same immutable design/Astro/tooling identities, corpus, fixture, asset manifest, resolved case and contract hash. Penpot bindings point to exact page/shape IDs, structural revision 1408, export revision 1409 and verified metadata read-back revision 1410. Historical 474 px cases are not active.

Each current case has a content-addressed 11-file durable pack (`astro.png`, `penpot.png`, overlay, diff, four structured checks, agent review, final receipt and run manifest), retention class `current-v2-long-term`. Total bounded size is recorded in `artifact-size-and-timing-summary.json`. Agent verdict is `MINOR`, not a false pixel PASS: framing/geometry and semantic actions conform; minor typography/baseline drift remains, plus a small place-ellipsis difference for fixture 4327. The still-open `event.meta.occurrence` semantic target is explicit P1 rather than hidden.

A real local run of one case passed. The exact seven-case batch then passed end-to-end through materialize → browser capture → identity → structural/raster comparison → hash-bound agent review → final receipt in 112 seconds. The review binding accepts the exact evidence bytes or a canonical decoded-pixel hash, so PNG encoding-only differences do not invalidate an unchanged visual review. No automatic image scaling is used.

## Actual Astro chip inventory

The inventory is generated by building all eight Golden Event Corpus v1 fixtures through the pinned Astro `EventCard.astro`, not inferred from prose documentation:

- event types: `выставка`, `концерт`, `лекция`;
- admission: `1000 ₽`, `1500 ₽`, `Бесплатно · вход свободный`, `Бесплатно · регистрация`, `Билеты`, `Запись по телефону`, `Условия уточняются`;
- eight exact occurrence labels;
- Calendar present/absent;
- Like absent/zero/positive counts and Share absent/zero/positive counts;
- fixed action labels `Не интересно`, `Поделиться`, `В календарь`;
- reachable admission generator families: sold-out, free-booking, donation, phone, ticket, free, price, arbitrary and unspecified.

Event type and admission remain semantic component masters with arbitrary text overrides; a component is not duplicated for each literal. Missing Penpot corpus specimens (`лекция`, `1500 ₽`, registration, unspecified, share=5) are explicit coverage gaps.

## External-audit reconciliation

The audit directly exposed the important integration failures that the earlier visual work did not prove: stale joins, advisory-only CI, `cases=[]` being misread as PASS, non-durable evidence, mutable/cross-repo pins, unreachable historical SHAs and ambiguous legacy review targets. It also forced the runtime compatibility check that found the design CLI and events runner commands did not actually match. Those findings were converted into fail-closed validators, the real reusable workflow, stale-unreproducible records, explicit supersession/routing, a single seven-case registry and durable evidence packs. Audit claims that mobile cases were wholly absent were corrected: the cases existed, but were not in one reproducible current registry.

## Verification

- `node tests/event-card-large-current-v2.test.mjs` — PASS.
- `node tests/ui-archetype-context-v1.test.mjs` — PASS.
- `node tests/ui-conformance-v1.test.mjs` — PASS.
- normalization medallion/navigation test — 20/20 PASS.
- one actual local current-v2 case — `minor`, PASS through final receipt, 18.9 s.
- actual seven-case local batch — 7/7 `minor`, 112 s wall clock.
- cleanup `--dry-run` — PASS; all seven verified long-term runs kept, none removed.
- `git diff --check` and workflow YAML parse — PASS before commit.

No production UI, deploy, family promotion, Penpot comment resolution, owner lifecycle promotion or Telegram publication was performed. PR #40 and #546 were marked superseded through immutable comments; history was not rewritten.
