# R3 contract-map result

The package reuses `family.event-preview-representations`, `event.card`, `event.media-frame` and `listing.rail-row`; it does not mutate the immutable decoder candidate. Current evidence requires separate candidate boundaries for compact listing, festival and exhibition implementations. `event.list-item` remains unmaterialized.
